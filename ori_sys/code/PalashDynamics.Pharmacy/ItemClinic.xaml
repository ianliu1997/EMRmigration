<controls:ChildWindow xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk"  xmlns:toolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit"  x:Class="PalashDynamics.Pharmacy.ItemClinic"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
           xmlns:controls="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls"
           Width="400" Height="400"  Loaded="ChildWindow_Loaded"
           xmlns:Grp="clr-namespace:Groupbox;assembly=Groupbox"
                        xmlns:inputcombo="clr-namespace:PalashDynamics.Controls;assembly=PalashDynamics.Controls.AutoCompleteComboBox"
           Title="Item Store">
    <controls:ChildWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/PalashDynamics;component/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </controls:ChildWindow.Resources>

    
    
    <Grid>
        <toolkit:DockPanel x:Name="MainDock" LastChildFill="True" toolkit:DockPanel.Dock="Top">
            <Border BorderBrush="{StaticResource TabControlDarkerBorder}"   BorderThickness="1" CornerRadius="3,3,0,0" Margin="4,4,4,4"  Background="Transparent">
                <toolkit:DockPanel toolkit:DockPanel.Dock="Top" LastChildFill="True">
                    <toolkit:DockPanel toolkit:DockPanel.Dock="Bottom" Margin="4">
                        <StackPanel x:Name="FooterBackPanel" Orientation="Horizontal"    Margin="0,3,5,3" HorizontalAlignment="Right" Background="Transparent">
                            <StackPanel x:Name="SaveCommandPanelBackPanel" Orientation="Horizontal"  Margin="0,0,0,0">
                                <ToggleButton x:Name="OKButton" Click="OKButton_Click"  Content="Save"   Background="#000000" Margin="5,0,0,0" HorizontalAlignment="Stretch" Style="{StaticResource ToggleButtonStyle}" Visibility="Visible"  />
                                <ToggleButton x:Name="CloseButton" Click="CancelButton_Click"  Content="Close"  Background="#000000" Margin="5,0,0,0" HorizontalAlignment="Stretch" Style="{StaticResource ToggleButtonStyle}" Visibility="Visible"   />
                            </StackPanel>
                        </StackPanel>
                    </toolkit:DockPanel>

                    <toolkit:DockPanel toolkit:DockPanel.Dock="Top" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="4">
                      
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="auto"></RowDefinition>
                                    <RowDefinition Height="auto"></RowDefinition>
                                    <RowDefinition Height="auto"></RowDefinition>
                                    <RowDefinition Height="auto"></RowDefinition>
                                    <RowDefinition ></RowDefinition>
                                    <RowDefinition ></RowDefinition>
                                    <RowDefinition></RowDefinition>
                                    <RowDefinition></RowDefinition>

                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto" ></ColumnDefinition>
                                    <ColumnDefinition ></ColumnDefinition>
                                    <ColumnDefinition ></ColumnDefinition>
                                    <ColumnDefinition ></ColumnDefinition>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Item Name" Grid.Column="0" Grid.Row="0" Margin="4"></TextBlock>


                                <TextBlock x:Name="lblItemName"   VerticalAlignment="Center" HorizontalAlignment="Stretch" Grid.Column="1" Grid.Row="0" Margin="4"></TextBlock>
                                <TextBlock Text="Clinic Name" VerticalAlignment="Center" HorizontalAlignment="Stretch" Grid.Column="0" Grid.Row="1" Margin="4"></TextBlock>
                                <inputcombo:AutoCompleteComboBox x:Name="cboItemClinic" SelectionChanged="cboItemClinic_SelectionChanged" Grid.ColumnSpan="2" SelectedItemBinding="{Binding MasterListItem, Mode=OneWay}" SelectedValuePath="ID"     Grid.Row="1" Grid.Column="1"  Margin="4" HorizontalAlignment="Stretch"></inputcombo:AutoCompleteComboBox>
                                <TextBlock Text="Apply Store" VerticalAlignment="Center" HorizontalAlignment="Stretch" Grid.Column="0" Grid.Row="4" Margin="4"></TextBlock>
                                <Grid Grid.Row="4" Grid.Column="1" Grid.RowSpan="4" Grid.ColumnSpan="4" >
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="auto"></RowDefinition>
                                        <RowDefinition></RowDefinition>
                                       
                                       
                                    </Grid.RowDefinitions>
                                    <CheckBox x:Name="chkAllStores" Margin="4" Grid.Row="0" Content="Select All" IsChecked="{Binding CheckedAllTariffs,Mode=TwoWay}" Checked="chkAllStores_Checked"  Unchecked="chkAllStores_Unchecked"   ></CheckBox>
                                    <ListBox x:Name="lstStores"   Grid.Row="1" ScrollViewer.VerticalScrollBarVisibility="Visible"  Grid.Column="1" Width="auto"   Height="auto"  >
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                            <CheckBox x:Name="chkStores" IsChecked="{Binding status,Mode=TwoWay}" Content="{Binding StoreName}" Tag="{Binding ID}"  Checked="chkStores_Checked"   Unchecked="chkStores_Unchecked"     ></CheckBox>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                </Grid>

                            </Grid>
                       
                    </toolkit:DockPanel>
                    
                    <!--<toolkit:DockPanel toolkit:DockPanel.Dock="Top" Margin="4">
                        <Grp:GroupBox Header="List Of Items">
                            <sdk:DataGrid Visibility="Visible" SelectionChanged="dgItemClinic_SelectionChanged"      HorizontalAlignment="Stretch"  VerticalAlignment="Stretch" ScrollViewer.VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Visible" AlternatingRowBackground="Silver" AutoGenerateColumns="false" x:Name="dgItemClinic"  >
                                <sdk:DataGrid.Columns>


                                    <sdk:DataGridTextColumn IsReadOnly="True" Binding="{Binding ItemCode}" Header="Item Code" />
                                    <sdk:DataGridTextColumn IsReadOnly="True" Binding="{Binding ItemName}" Header="Item Name" />
                                    <sdk:DataGridTextColumn IsReadOnly="True" Binding="{Binding ClinicName}" Header="Clinic" />
                                    <sdk:DataGridTextColumn IsReadOnly="True" Binding="{Binding StoreName}" Header="Store" />

                                    <sdk:DataGridTemplateColumn Header="Status"   >
                                        <sdk:DataGridTemplateColumn.CellEditingTemplate >
                                            <DataTemplate>
                                                <CheckBox x:Name="chkStatus"  HorizontalAlignment="Center" Tag="{Binding ID}"   IsChecked="{Binding Status,Mode=TwoWay}"></CheckBox>
                                            </DataTemplate>
                                        </sdk:DataGridTemplateColumn.CellEditingTemplate>
                                    </sdk:DataGridTemplateColumn>
                                    <sdk:DataGridTextColumn IsReadOnly="True" Visibility="Collapsed" Binding="{Binding ClinicID}" />

                                    <sdk:DataGridTextColumn IsReadOnly="True" Visibility="Collapsed" Binding="{Binding ID}" />
                                    <sdk:DataGridTextColumn IsReadOnly="True" Visibility="Collapsed" Binding="{Binding ItemID}" />



                                </sdk:DataGrid.Columns>
                            </sdk:DataGrid>
                        </Grp:GroupBox>
                    </toolkit:DockPanel>-->
                </toolkit:DockPanel>
            </Border>
        </toolkit:DockPanel>
    </Grid>
</controls:ChildWindow>