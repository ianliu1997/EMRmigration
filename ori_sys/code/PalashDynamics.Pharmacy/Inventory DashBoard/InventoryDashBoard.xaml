<UserControl xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk"  xmlns:toolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit"  x:Class="PalashDynamics.Pharmacy.InventoryDashBoard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:blacklight="clr-namespace:Blacklight.Silverlight.Controls;assembly=Blacklight.Silverlight.Controls"
    xmlns:Converter="clr-namespace:PalashDynamics.Converters;assembly=PalashDynamics.Converters"
    d:DesignHeight="300" d:DesignWidth="400">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/PalashDynamics;component/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Converter:DateTimeConverter x:Key="DateConverter"></Converter:DateTimeConverter>

        </ResourceDictionary>
    </UserControl.Resources>
    <Grid x:Name="Root">
        <Grid x:Name="LayoutRoot" Background="WhiteSmoke" >
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <!--<blacklight:DragDockPanelHost x:Name="DragPanelContainer">-->
<!--Commented By Umesh-->
                
                <!--<blacklight:DragDockPanel Margin="5" Style="{StaticResource DragDockPanelStyle}" >
                    <Grid >
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <TextBlock FontSize="12" Foreground="Black" Margin="5, 2, 0, 0" IsHitTestVisible="False">Pending Indents</TextBlock>

                        <Grid Grid.Row="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>

                            <RadioButton x:Name="rdbIndentAsc" Content="Ascending" Grid.Row="0" Grid.Column="1" Margin="4" HorizontalAlignment="Right" Checked="rdbIndentAsc_Click">
                            </RadioButton>

                            <RadioButton x:Name="rdbIndentDesc" Content="Descending" Grid.Row="0" Grid.Column="2" Margin="4" HorizontalAlignment="Left" Checked="rdbIndentDesc_Click">
                            </RadioButton>

                            <toolkit:DockPanel  Grid.Row="1" Grid.ColumnSpan="3">
                                <sdk:DataPager x:Name="dataGridIndentPager"  toolkit:DockPanel.Dock="Bottom"
                                            NumericButtonCount="10" AutoEllipsis="False" 
                                             PageSize="{Binding PageSize}"
                                            DisplayMode="FirstLastPreviousNext"
                                            IsEnabled="True"
                                            IsTotalItemCountFixed="True"
                                            Source="{Binding ItemIndentList}"
                                             />

                                
                                <sdk:DataGrid  IsReadOnly="True" ItemsSource="{Binding}"  x:Name="dgIndentList"  AutoGenerateColumns="False" Height="250" >
                                    <sdk:DataGrid.Columns>

                                        <sdk:DataGridTemplateColumn  Header="Close">
                                            <sdk:DataGridTemplateColumn.CellTemplate >
                                                <DataTemplate >
                                                    
                                                    <CheckBox Margin="10,2,10,2"  x:Name="chkCloseIndent" IsEnabled="{Binding isIndentClosed,Mode=TwoWay}" IsTabStop="True"   Click="chkCloseIndent_Click" IsChecked="{Binding IndentDashBordStatus,Mode=TwoWay}" VerticalAlignment="Center" HorizontalAlignment="Center" ></CheckBox >
                                                </DataTemplate>
                                            </sdk:DataGridTemplateColumn.CellTemplate>
                                        </sdk:DataGridTemplateColumn>
                                        <sdk:DataGridTextColumn IsReadOnly="True" Header="Indent No" Binding="{Binding IndentNumber}"/>
                                        <sdk:DataGridTextColumn IsReadOnly="True" Header="Date" Width="150"  Binding="{Binding Date,Converter={StaticResource DateConverter},ConverterParameter={StaticResource GridDateDisplayFormat}}"/>
                                        <sdk:DataGridTextColumn IsReadOnly="True" Header="Due Date" Width="100"  Binding="{Binding DueDate,Converter={StaticResource DateConverter},ConverterParameter={StaticResource GridDateDisplayFormat}}"/>
                                        <sdk:DataGridTextColumn IsReadOnly="True" Header="From Store" Binding="{Binding FromStoreName}"/>
                                        <sdk:DataGridTextColumn IsReadOnly="True" Header="To Store " Width="170" Binding="{Binding ToStoreName}"/>
                                        
                                        <sdk:DataGridTextColumn IsReadOnly="True" Header="Status" Width="100" Binding="{Binding IndentStatusName}"/>
                                    </sdk:DataGrid.Columns>
                                </sdk:DataGrid>
                            </toolkit:DockPanel >
                        </Grid>
                    </Grid>
                </blacklight:DragDockPanel>
                <blacklight:DragDockPanel Margin="5" Style="{StaticResource DragDockPanelStyle}">
                    <Grid >
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <TextBlock FontSize="12" Foreground="Black" Margin="5, 2, 0, 0" IsHitTestVisible="False">Pending Purchase Orders</TextBlock>

                        <Grid Grid.Row="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>

                            <RadioButton x:Name="rdbPendingPOAsc" Content="Ascending" Grid.Row="0" Grid.Column="1" Margin="4" HorizontalAlignment="Right" Checked="rdbPendingPOAsc_Click">
                            </RadioButton>

                            <RadioButton x:Name="rdbPendingPODesc" Content="Descending" Grid.Row="0" Grid.Column="2" Margin="4" HorizontalAlignment="Left" Checked="rdbPendingPODesc_Click">
                            </RadioButton>

                            <toolkit:DockPanel  Grid.Row="1" Grid.ColumnSpan="3">
                                
                                <sdk:DataPager x:Name="grdPendingPO" toolkit:DockPanel.Dock="Bottom" Source="{Binding PurchaseOrderList}" NumericButtonCount="10" AutoEllipsis="False"/>
                                <sdk:DataGrid x:Name="dgPendingPO"  IsReadOnly="True" SelectionMode="Single"  ItemsSource="{Binding}"  AutoGenerateColumns="False" Height="250">
                                    <sdk:DataGrid.Columns>

                                       


                                        <sdk:DataGridTextColumn Binding="{Binding PONO}" Header="PO NO."/>
                                        <sdk:DataGridTextColumn Binding= "{Binding Date,Converter={StaticResource DateConverter},ConverterParameter={StaticResource GridDateDisplayFormat}}" Header="Date" />
                                        <sdk:DataGridTextColumn Binding="{Binding TotalAmount,StringFormat=0.000}" Header="Total Amount">
                                            <sdk:DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="TextAlignment" Value="Right"/>
                                                </Style>
                                            </sdk:DataGridTextColumn.ElementStyle>
                                        </sdk:DataGridTextColumn>
                                        <sdk:DataGridTextColumn Binding="{Binding SupplierName}" Header="Supplier Name" />
                                        <sdk:DataGridTextColumn Binding="{Binding DeliveryLocation}" Header="Delivery Location"/>

                                    </sdk:DataGrid.Columns>
                                </sdk:DataGrid>
                            </toolkit:DockPanel >
                        </Grid>
                    </Grid>
                </blacklight:DragDockPanel>
                
                <blacklight:DragDockPanel Margin="5" Style="{StaticResource DragDockPanelStyle}" >
                    <Grid >
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <TextBlock FontSize="12" Foreground="Black" Margin="5, 2, 0, 0" IsHitTestVisible="False">Reorder List</TextBlock>

                        <Grid Grid.Row="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>

                            

                            <toolkit:DockPanel  Grid.Row="1" Grid.ColumnSpan="3">
                                <sdk:DataPager x:Name="dataGridItemReorderPager"  toolkit:DockPanel.Dock="Bottom"
                                            NumericButtonCount="10" AutoEllipsis="False" 
                                             PageSize="{Binding PageSize}"
                                            DisplayMode="FirstLastPreviousNext"
                                            IsEnabled="True"
                                            IsTotalItemCountFixed="True"
                                            Source="{Binding ReorderItemList}"
                                             />


                                <sdk:DataGrid  IsReadOnly="True" SelectionMode="Single" ItemsSource="{Binding}"  x:Name="dgItemReorderList"  AutoGenerateColumns="False" Height="250" >
                                    <sdk:DataGrid.Columns>
                                        <sdk:DataGridTextColumn IsReadOnly="True" Header="Item Name" Binding="{Binding ItemName}"/>
                                        <sdk:DataGridTextColumn IsReadOnly="True" Header="Batch Code" Binding="{Binding BatchCode}"/>
                                        <sdk:DataGridTextColumn IsReadOnly="True" Header="Expiry Date" Binding="{Binding ExpiryDate,Converter={StaticResource DateConverter},ConverterParameter={StaticResource GridDateDisplayFormat}}"/>
                                        <sdk:DataGridTextColumn IsReadOnly="True" Header="Available Stock" Binding="{Binding AvailableStock}"/>
                                        <sdk:DataGridTextColumn IsReadOnly="True" Header="Reorder Quantity" Binding="{Binding ItemReorderQty}"/>

                                    </sdk:DataGrid.Columns>
                                </sdk:DataGrid>
                            </toolkit:DockPanel >
                        </Grid>
                    </Grid>
                </blacklight:DragDockPanel>-->
<!--Commented By Umesh End-->            
                
                <!--<blacklight:DragDockPanel x:Name="fccProcedure" Margin="5" Style="{StaticResource DragDockPanelStyle}" Maximized="ExpiredItems_Maximized"  Minimized="ExpiredItems_Minimized">-->
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <!--<TextBlock FontSize="12" Foreground="Black" Margin="5, 2, 0, 0" IsHitTestVisible="False">Current Expired Items</TextBlock>-->
<ContentControl VerticalAlignment="Center" Grid.ColumnSpan="4" FontSize="12" Content="Current Expired Items"  Style="{StaticResource Header}"  Margin="-1"    HorizontalAlignment="Stretch" />
                        <Grid Grid.Row="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <!--<RowDefinition/>-->
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <!--
                            <Button x:Name="btnExpiredItemsAsc" Width="Auto" Height="20" Grid.Row="0" Grid.Column="1" Content="Expired Items Ascending" Click="btnExpiredItemsAsc_Click" BorderThickness="2" BorderBrush="Black" />
                            <Button x:Name="btnExpiredItemsDesc" Width="Auto" Height="20" Grid.Row="0" Grid.Column="2" Content="Expired Items Descending" Click="btnExpiredItemsDesc_Click" BorderThickness="2" BorderBrush="Black" />
                            -->
                            <!--  <Button x:Name="btnExpiredItemsAsc" Grid.Row="0" Grid.Column="1" Margin="4" Click="btnExpiredItemsAsc_Click" TabIndex="6" HorizontalAlignment="Right" >
                                <Button.Content>
                                    <TextBlock Text="5" FontSize="18" FontFamily="Webdings" ToolTipService.ToolTip="Move Up"></TextBlock>
                                </Button.Content>
                            </Button>

                            <Button x:Name="btnExpiredItemsDesc" Grid.Row="0" Grid.Column="2" Margin="4" Click="btnExpiredItemsDesc_Click" TabIndex="6" HorizontalAlignment="Left" >
                                <Button.Content>
                                    <TextBlock Text="5" FontSize="18" FontFamily="Webdings" ToolTipService.ToolTip="Move Up"></TextBlock>
                                </Button.Content>
                            </Button> -->

                            <RadioButton x:Name="rdbExpiredItemsAsc" Content="Ascending" Grid.Row="0" Grid.Column="0" Margin="4" HorizontalAlignment="Left" Checked="btnExpiredItemsAsc_Click">
                            </RadioButton>

                            <RadioButton x:Name="rdbExpiredItemsDesc" Content="Descending" Grid.Row="0" Grid.Column="1" Margin="4" HorizontalAlignment="Left" Checked="btnExpiredItemsDesc_Click">
                            </RadioButton>

                            <toolkit:DockPanel  Grid.Row="1" Grid.ColumnSpan="3">
                                <sdk:DataPager x:Name="DataPagerExpired"  toolkit:DockPanel.Dock="Bottom" Source="{Binding ExpiryItemList}" NumericButtonCount="10" AutoEllipsis="False"/>
                                <sdk:DataGrid FrozenColumnCount="1" IsReadOnly="True"  SelectionMode="Single" x:Name="dgExpiredListGrid" ItemsSource="{Binding}"  AutoGenerateColumns="False" Height="250">
                                    <sdk:DataGrid.Columns>
                                        <sdk:DataGridTextColumn  Binding="{Binding ItemName}" Header="Item Name" />
                                        <sdk:DataGridTextColumn  Binding="{Binding ItemCode}" Header="Item Code" />
                                        <sdk:DataGridTextColumn  Binding="{Binding BatchCode}" Header="Batch Code" />
                                        <sdk:DataGridTextColumn  Binding="{Binding AvailableStock,StringFormat=0.00}"  Header="Today's Available Stock">
                                            <sdk:DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="TextAlignment" Value="Right"/>
                                                </Style>
                                            </sdk:DataGridTextColumn.ElementStyle>
                                        </sdk:DataGridTextColumn>
                                        <sdk:DataGridTextColumn  Binding="{Binding BatchExpiryDate,Converter={StaticResource DateConverter},ConverterParameter={StaticResource ExpiryDateDisplayFormat}}"  Header="Expiry Date" />
                                    </sdk:DataGrid.Columns>
                                </sdk:DataGrid>

                            </toolkit:DockPanel >
                            <!--<sdk:DataPager x:Name="DataPagerExpired" toolkit:DockPanel.Dock="Bottom" Grid.Row="2" Grid.ColumnSpan="3" Source="{Binding ExpiryItemList}" NumericButtonCount="10" AutoEllipsis="False"/>-->
                        </Grid>

                    </Grid>
                <!--</blacklight:DragDockPanel>-->
<!--Commented By Umesh-->
                <!--By Anjali-->
                <!--<blacklight:DragDockPanel Margin="5" Style="{StaticResource DragDockPanelStyle}" >
                    <Grid >
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <TextBlock FontSize="12" Foreground="Black" Margin="5, 2, 0, 0" IsHitTestVisible="False">Pending Purchase Requisition</TextBlock>

                        <Grid Grid.Row="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>

                            <RadioButton x:Name="rdbPRAsc" Content="Ascending" Grid.Row="0" Grid.Column="1" Margin="4" HorizontalAlignment="Right" Checked="rdbPRAsc_Click">
                            </RadioButton>

                            <RadioButton x:Name="rdbPRDesc" Content="Descending" Grid.Row="0" Grid.Column="2" Margin="4" HorizontalAlignment="Left" Checked="rdbPRDesc_Click">
                            </RadioButton>

                            <toolkit:DockPanel  Grid.Row="1" Grid.ColumnSpan="3">
                                <sdk:DataPager x:Name="dataGridPRPager"  toolkit:DockPanel.Dock="Bottom"
                                            NumericButtonCount="10" AutoEllipsis="False" 
                                             PageSize="{Binding PageSize}"
                                            DisplayMode="FirstLastPreviousNext"
                                            IsEnabled="True"
                                            IsTotalItemCountFixed="True"
                                            Source="{Binding ItemIndentList}"
                                             />

                                <sdk:DataGrid  IsReadOnly="True" ItemsSource="{Binding}"  x:Name="dgPRList"  AutoGenerateColumns="False" Height="250" >
                                    <sdk:DataGrid.Columns>

                                        <sdk:DataGridTemplateColumn  Header="Close">
                                            <sdk:DataGridTemplateColumn.CellTemplate >
                                                <DataTemplate >
                                                    
                                                    <CheckBox Margin="10,2,10,2"  x:Name="chkClosePR" IsEnabled="{Binding isIndentClosed,Mode=TwoWay}" IsTabStop="True"   Click="chkClosePR_Click" IsChecked="{Binding IndentDashBordStatus,Mode=TwoWay}" VerticalAlignment="Center" HorizontalAlignment="Center" ></CheckBox >
                                                </DataTemplate>
                                            </sdk:DataGridTemplateColumn.CellTemplate>
                                        </sdk:DataGridTemplateColumn>
                                        <sdk:DataGridTextColumn IsReadOnly="True" Header="Purchase Requisition No" Binding="{Binding IndentNumber}"/>
                                        <sdk:DataGridTextColumn IsReadOnly="True" Header="Date" Width="150"  Binding="{Binding Date,Converter={StaticResource DateConverter},ConverterParameter={StaticResource GridDateDisplayFormat}}"/>
                                        <sdk:DataGridTextColumn IsReadOnly="True" Header="Due Date" Width="100"  Binding="{Binding DueDate,Converter={StaticResource DateConverter},ConverterParameter={StaticResource GridDateDisplayFormat}}"/>
                                        <sdk:DataGridTextColumn IsReadOnly="True" Header="From Store" Binding="{Binding FromStoreName}"/>
                                        <sdk:DataGridTextColumn IsReadOnly="True" Header="To Store " Width="170" Binding="{Binding ToStoreName}"/>
                                        
                                        <sdk:DataGridTextColumn IsReadOnly="True" Header="Status" Width="100" Binding="{Binding IndentStatusName}"/>
                                    </sdk:DataGrid.Columns>
                                </sdk:DataGrid>
                            </toolkit:DockPanel >
                        </Grid>
                    </Grid>
                </blacklight:DragDockPanel>-->
                <!--End-->
<!--Commented By Umesh End-->
            <!--</blacklight:DragDockPanelHost>-->
        </Grid>
    </Grid>
</UserControl>
