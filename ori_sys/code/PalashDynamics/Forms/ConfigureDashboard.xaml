<controls:ChildWindow x:Class="CIMS.Forms.ConfigureDashboard"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
           xmlns:controls="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls"
           Width="400" Height="300" 
                      Title="Configure Dashboard" Loaded="ChildWindow_Loaded">
    <Grid x:Name="LayoutRoot" Margin="2">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid>
            <Border Margin="4" Grid.Row="8" Grid.ColumnSpan="7">
                <Grid>
                    <ListBox x:Name="lstItems"  ItemsSource="{Binding DashBoardList}" HorizontalAlignment="Stretch"  Margin="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" x:Name="stack1">
                                    <CheckBox x:Name="DashboardChecked" IsChecked="{Binding Status, Mode=TwoWay}"></CheckBox>
                                    <TextBlock Text="{Binding Description}"></TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
            <!--<ListBox>
                <ListBox.Items>
                    <ListBoxItem>
                        <ListBoxItem.Content>
                            <CheckBox IsChecked="True" Content="Recent Patients"></CheckBox>
                        </ListBoxItem.Content>
                    </ListBoxItem>
                    <ListBoxItem>
                        <ListBoxItem.Content>
                            <CheckBox IsChecked="True" Content="Appointments"></CheckBox>
                        </ListBoxItem.Content>
                    </ListBoxItem>
                    <ListBoxItem>
                        <ListBoxItem.Content>
                            <CheckBox IsChecked="True" Content="Daily Collection"></CheckBox>
                        </ListBoxItem.Content>
                    </ListBoxItem>
                    <ListBoxItem>
                        <ListBoxItem.Content>
                            <CheckBox IsChecked="True" Content="Alerts"></CheckBox>
                        </ListBoxItem.Content>
                    </ListBoxItem>
                    <ListBoxItem>
                        <ListBoxItem.Content>
                            <CheckBox IsChecked="False" Content="Patient Queue"></CheckBox>
                        </ListBoxItem.Content>
                    </ListBoxItem>
                                  <ListBoxItem>
                        <ListBoxItem.Content>
                            <CheckBox IsChecked="False" Content="Reminders"></CheckBox>
                        </ListBoxItem.Content>
                    </ListBoxItem>
                </ListBox.Items>
            </ListBox>-->
        </Grid>
        <Button x:Name="CancelButton" Content="Cancel" Click="CancelButton_Click" Width="75" Height="23" HorizontalAlignment="Right" Margin="0,12,0,0" Grid.Row="1" />
        <Button x:Name="OKButton" Content="OK" Click="OKButton_Click" Width="75" Height="23" HorizontalAlignment="Right" Margin="0,12,79,0" Grid.Row="1" />
    </Grid>
</controls:ChildWindow>