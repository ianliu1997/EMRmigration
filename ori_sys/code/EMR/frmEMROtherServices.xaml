<UserControl x:Class="EMR.frmEMROtherServices"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
           xmlns:controls="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls"
           xmlns:inputcombo="clr-namespace:PalashDynamics.Controls;assembly=PalashDynamics.Controls.AutoCompleteComboBox"
           xmlns:input="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Input"
           xmlns:controlsToolkit="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Toolkit"  
           xmlns:data="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Data"
           xmlns:Converter="clr-namespace:PalashDynamics.Converters;assembly=PalashDynamics.Converters"
           xmlns:Localization="clr-namespace:PalashDynamic.Localization;assembly=PalashDynamic.Localization"
             xmlns:toolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit"
           xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/PalashDynamics;component/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Localization:Localized x:Key="locResource"></Localization:Localized>
            <Converter:ToggleBooleanValueConverter x:Key="NegateValueConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid x:Name="LayoutRoot" Background="White">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition  ></RowDefinition>
                <RowDefinition Height="Auto" ></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Grid Margin="4" Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition ></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                </Grid.RowDefinitions>

                <StackPanel x:Name="DockPanelFooter" Orientation="Horizontal" VerticalAlignment="Center" Grid.Row="1"  Margin="0,3,5,3" HorizontalAlignment="Left" Background="Transparent">
                    <ToggleButton x:Name="lnkOtherServices" Content="{Binding btnOtherServices, Source={StaticResource locResource}}"  Grid.Row="1" Grid.Column="0" Click="cmdOtherServices_Click" Background="#000000" Margin="4" HorizontalAlignment="Left" Style="{StaticResource ToggleButtonStyle}" Visibility="Visible" Width="auto"></ToggleButton>
                </StackPanel>
                <Border Grid.Row="0" Grid.Column="0" BorderBrush="{StaticResource TabControlDarkerBorder}"   BorderThickness="1" CornerRadius="3,3,0,0" Background="Transparent">
                    <ContentControl VerticalAlignment="Center" FontSize="12" Content="{Binding lblCurrentDiagnosticServices, Source={StaticResource locResource}}" Margin="-1" Style="{StaticResource Header}" HorizontalAlignment="Stretch" />
                </Border>
                <data:DataGrid Grid.Row="2" HorizontalAlignment="Stretch"  Grid.ColumnSpan="2" x:Name="dgOtherServices" AutoGenerateColumns="False" ItemsSource="{Binding}" VerticalScrollBarVisibility="Visible" IsReadOnly="True" Margin="0"  TabIndex="2"  CanUserSortColumns="True" >
                    <data:DataGrid.Columns >
                        <data:DataGridTextColumn Binding="{Binding ServiceCode}" Localization:DataGridColumnHelper.HeaderBinding="{Binding lblCode, Source={StaticResource locResource}}" ></data:DataGridTextColumn>
                        <data:DataGridTextColumn Binding="{Binding ServiceName}"  Localization:DataGridColumnHelper.HeaderBinding="{Binding lblServiceName, Source={StaticResource locResource}}" ></data:DataGridTextColumn>

                        <data:DataGridTemplateColumn Localization:DataGridColumnHelper.HeaderBinding="{Binding lblPriority, Source={StaticResource locResource}}" MinWidth="100" >
                            <data:DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <inputcombo:AutoCompleteComboBox x:Name="cmbPriority" SelectedValue="{Binding SelectedPriority.ID}" SelectedItem="{Binding SelectedPriority,Mode=TwoWay}" ItemsSource="{Binding Priority}" Margin="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
                                    </inputcombo:AutoCompleteComboBox>
                                </DataTemplate>
                            </data:DataGridTemplateColumn.CellEditingTemplate>
                        </data:DataGridTemplateColumn>
                        <data:DataGridTemplateColumn>
                            <data:DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <HyperlinkButton VerticalAlignment="Center" FontFamily="Webdings" Content="r"  Padding="5,0,5,0" IsTabStop="False" x:Name="cmdDeleteDiagnosis" ToolTipService.ToolTip="Delete" Click="cmdDelete_Click" IsEnabled="{Binding IsEnabled,Converter={StaticResource NegateValueConverter}}" ></HyperlinkButton>
                                </DataTemplate>
                            </data:DataGridTemplateColumn.CellTemplate>
                        </data:DataGridTemplateColumn>

                    </data:DataGrid.Columns>
                </data:DataGrid>

                <Border Grid.Row="3"  BorderBrush="{StaticResource TabControlDarkerBorder}"  Margin="4" toolkit:DockPanel.Dock="Bottom" BorderThickness="1" CornerRadius="3,3,3,3" Background="Transparent" >
                    <StackPanel x:Name="Footer" Orientation="Horizontal" VerticalAlignment="Center"   Margin="0,3,5,3" HorizontalAlignment="Right" Background="Transparent">
                        <StackPanel Orientation="Horizontal"  Margin="1" HorizontalAlignment="Right"  >

                            <ToggleButton   Margin="5,0,10,0" Content="{Binding btnSave, Source={StaticResource locResource}}" Style="{StaticResource ToggleButtonStyle}" x:Name="cmdSave" Click="cmdSave_Click" Width="auto"></ToggleButton>
                            <ToggleButton  Margin="5,0,0,0" Content="{Binding btnCancel, Source={StaticResource locResource}}" Style="{StaticResource ToggleButtonStyle}" x:Name="cmdCancel" Click="cmdCancel_Click" Width="auto" ></ToggleButton>

                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>


            <toolkit:DockPanel Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="4">

                <data:DataPager x:Name="pgrPrevOtherServices" AutoEllipsis="False" PageSize="{Binding PageSize}" DisplayMode="FirstLastPreviousNext" IsEnabled="True" IsTotalItemCountFixed="True" toolkit:DockPanel.Dock="Bottom"/>

                <Border toolkit:DockPanel.Dock="Top" BorderBrush="{StaticResource TabControlDarkerBorder}"   BorderThickness="1" CornerRadius="3,3,0,0"  Background="Transparent">
                    <ContentControl VerticalAlignment="Center" FontSize="12" Content="{Binding lblPreviousDiagnosticServices, Source={StaticResource locResource}}" Margin="-1" Style="{StaticResource Header}"  HorizontalAlignment="Stretch" />
                </Border>

                <ScrollViewer Background="Transparent" VerticalScrollBarVisibility="Auto" toolkit:DockPanel.Dock="Top"   VerticalAlignment="Stretch" toolkit:ScrollViewerExtensions.IsMouseWheelScrollingEnabled="True">
                    <data:DataGrid HorizontalAlignment="Stretch" x:Name="dgPreviousOtherServices" AutoGenerateColumns="False" ItemsSource="{Binding}" VerticalScrollBarVisibility="Visible" IsReadOnly="True" TabIndex="2" CanUserSortColumns="True" >
                        <data:DataGrid.Columns >
                            <data:DataGridTextColumn Binding="{Binding ServiceCode,Mode=TwoWay}"  Localization:DataGridColumnHelper.HeaderBinding="{Binding lblCode, Source={StaticResource locResource}}" ></data:DataGridTextColumn>
                            <data:DataGridTextColumn Binding="{Binding ServiceName, Mode=TwoWay}"  Localization:DataGridColumnHelper.HeaderBinding="{Binding lblServiceName, Source={StaticResource locResource}}" ></data:DataGridTextColumn>

                            <data:DataGridTemplateColumn Localization:DataGridColumnHelper.HeaderBinding="{Binding lblPriority, Source={StaticResource locResource}}" MinWidth="100" >
                                <data:DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <inputcombo:AutoCompleteComboBox x:Name="cmbPriority" SelectedValue="{Binding SelectedPriority.ID}" SelectedItem="{Binding SelectedPriority,Mode=TwoWay}" ItemsSource="{Binding Priority}" Margin="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
                                        </inputcombo:AutoCompleteComboBox>
                                    </DataTemplate>
                                </data:DataGridTemplateColumn.CellEditingTemplate>
                            </data:DataGridTemplateColumn>
                            <data:DataGridTemplateColumn>
                                <data:DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <HyperlinkButton VerticalAlignment="Center" FontFamily="Webdings" Content="r"  Padding="5,0,5,0" IsTabStop="False" x:Name="cmdDeleteDiagnosis" ToolTipService.ToolTip="Delete" IsEnabled="{Binding IsEnabled,Converter={StaticResource NegateValueConverter}}" ></HyperlinkButton>
                                    </DataTemplate>
                                </data:DataGridTemplateColumn.CellTemplate>
                            </data:DataGridTemplateColumn>

                        </data:DataGrid.Columns>
                    </data:DataGrid>
                </ScrollViewer>
            </toolkit:DockPanel>

        </Grid>
    </Grid>
</UserControl>