<controls:ChildWindow  x:Class="EMR.frmEMRPrescribedMedication"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
           xmlns:data="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Data"
           xmlns:toolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit"  
           xmlns:controlsToolkit="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Toolkit" 
           xmlns:inputcombo="clr-namespace:PalashDynamics.Controls;assembly=PalashDynamics.Controls.AutoCompleteComboBox"
           xmlns:local="clr-namespace:PalashDynamics.Converters;assembly=PalashDynamics.Converters"
           xmlns:controls="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls"
           xmlns:dataInput="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Data.Input"
         xmlns:Localization="clr-namespace:PalashDynamic.Localization;assembly=PalashDynamic.Localization"
                       xmlns:input="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Input"
            Loaded="ChildWindow_Loaded">
    <controls:ChildWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/PalashDynamics;component/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Localization:Localized x:Key="locResource"></Localization:Localized>
            <local:DateTimeConverter x:Key="DateConverter" />
        </ResourceDictionary>
    </controls:ChildWindow.Resources>
    <Grid x:Name="LayoutRoot" Margin="2">
        <controlsToolkit:DockPanel x:Name="Main">
            <controlsToolkit:DockPanel x:Name="docFooter" controlsToolkit:DockPanel.Dock="Bottom">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="100"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" BorderBrush="{StaticResource TabControlDarkerBorder}"   BorderThickness="1" CornerRadius="3,3,0,0"  Background="Transparent">
                        <ContentControl VerticalAlignment="Center" FontSize="12" Content="List Of Selected Drug" Margin="-1" Style="{StaticResource Header}"     HorizontalAlignment="Stretch" />
                    </Border>
                    <data:DataGrid    Grid.Row="1" x:Name="dgDrugSelectedList"  Margin="2"  AutoGenerateColumns="False" ItemsSource="{Binding}"    HorizontalAlignment="Stretch" TabIndex="6" HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Visible">
                        <data:DataGrid.Columns>
                            <data:DataGridTemplateColumn>
                                <data:DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox x:Name="chkSelMultipleDrug" Margin="2" IsChecked="{Binding IsSelected, Mode=TwoWay}" Click="chkSelMultipleDrug_Click"></CheckBox>
                                    </DataTemplate>
                                </data:DataGridTemplateColumn.CellTemplate>
                            </data:DataGridTemplateColumn>
                            <data:DataGridTextColumn Binding="{Binding DrugName}"  Localization:DataGridColumnHelper.HeaderBinding="Drug"  IsReadOnly="True" ></data:DataGridTextColumn>

                            <data:DataGridTextColumn    Binding="{Binding Days}" Localization:DataGridColumnHelper.HeaderBinding="{Binding lblDays,Source={StaticResource locResource}}"  IsReadOnly="True" />


                            <data:DataGridTextColumn Visibility="Collapsed"  Binding="{Binding Frequency}" Localization:DataGridColumnHelper.HeaderBinding="Frequency" IsReadOnly="True" />

                            <data:DataGridTextColumn Visibility="Collapsed"  Binding="{Binding Quantity}" Localization:DataGridColumnHelper.HeaderBinding="Quantity" IsReadOnly="True" />

                            <data:DataGridTextColumn Visibility="Collapsed" Binding="{Binding Instruction}" Localization:DataGridColumnHelper.HeaderBinding="Instruction" IsReadOnly="True"/>
                        </data:DataGrid.Columns>
                    </data:DataGrid>
                    <controlsToolkit:DockPanel Grid.Row="2" Margin="2" controlsToolkit:DockPanel.Dock="Bottom">
                        <StackPanel Grid.Row="3" x:Name="Footer" Orientation="Horizontal" VerticalAlignment="Center"   Margin="0,3,5,3" HorizontalAlignment="Right" Background="Transparent">
                            <StackPanel x:Name="SaveCommandPanel" Orientation="Horizontal"  Margin="0,0,0,0">
                                <ToggleButton  x:Name="cmdAdd"  Grid.Row="4" Content="Add" MinWidth="50" Background="#000000" Margin="5,0,0,0" HorizontalAlignment="Stretch"  Visibility="Visible" Style="{StaticResource ToggleButtonStyle}" Click="OKButton_Click" TabIndex="7" />
                                <ToggleButton  x:Name="cmdClose"  Grid.Row="4" Content="Cancel" MinWidth="50" Background="#000000" Margin="5,0,0,0" HorizontalAlignment="Stretch"  Visibility="Visible"  Style="{StaticResource ToggleButtonStyle}" Click="CancelButton_Click" TabIndex="8"/>
                            </StackPanel>
                        </StackPanel>
                    </controlsToolkit:DockPanel>
                </Grid>
            </controlsToolkit:DockPanel>
            <controlsToolkit:DockPanel x:Name="serviceOrderList1" >
                <controlsToolkit:DockPanel x:Name="serviceList">
                    <controlsToolkit:DockPanel x:Name="DockTop" controlsToolkit:DockPanel.Dock="Top" HorizontalAlignment="Stretch">
                        <Grid x:Name="LayoutRoot2" Background="White" Margin="4" HorizontalAlignment="Stretch"  >
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"></RowDefinition>
                                <RowDefinition Height="100"></RowDefinition>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" BorderBrush="{StaticResource TabControlDarkerBorder}"   BorderThickness="1" CornerRadius="3,3,0,0"  Background="Transparent">
                                <ContentControl VerticalAlignment="Center" FontSize="12" Content="{Binding lblPreviousVisitList, Source={StaticResource locResource}}" Margin="-1" Style="{StaticResource Header}" Name="LBLHeader"     HorizontalAlignment="Stretch" />
                            </Border>
                            <data:DataGrid  Grid.Row="1" x:Name="dgVisitDetails" Margin="2,2,2,0" ItemsSource="{Binding}"  AutoGenerateColumns="False" IsReadOnly="True" SelectionChanged="dgVisitDetails_SelectionChanged">
                                <data:DataGrid.Columns>
                                    <data:DataGridTextColumn Binding="{Binding VisitDate,Converter={StaticResource DateConverter},ConverterParameter={StaticResource GridDateDisplayFormat}}" Localization:DataGridColumnHelper.HeaderBinding="{Binding lblVisitDate,Source={StaticResource locResource}}" />
                                    <data:DataGridTextColumn Binding="{Binding Doctor}"  Localization:DataGridColumnHelper.HeaderBinding="{Binding lblDoctor,Source={StaticResource locResource}}" />
                                    <data:DataGridTextColumn Binding="{Binding OPDNO}"  Localization:DataGridColumnHelper.HeaderBinding="OPD/IPD" />
                                </data:DataGrid.Columns>
                            </data:DataGrid>
                        </Grid>
                    </controlsToolkit:DockPanel>
                    <StackPanel controlsToolkit:DockPanel.Dock="Top">
                        <Border  BorderBrush="{StaticResource TabControlDarkerBorder}"   BorderThickness="1" CornerRadius="3,3,0,0"  Background="Transparent">
                            <ContentControl VerticalAlignment="Center" FontSize="12" Style="{StaticResource Header}" Content="{Binding lblListOfDrug,Source={StaticResource locResource}}" Margin="-1" HorizontalAlignment="Stretch" />
                        </Border>
                    </StackPanel>
                    <data:DataGrid    Grid.Row="0" x:Name="dgDrugList" Margin="2"  AutoGenerateColumns="False" ItemsSource="{Binding}"    HorizontalAlignment="Stretch" TabIndex="6" HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Visible">
                        <data:DataGrid.Columns>
                            <data:DataGridTemplateColumn>
                                <data:DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox x:Name="chkMultipleServices"  Margin="2" IsChecked="{Binding IsSelected, Mode=TwoWay}" Click="chkMultipleDrug_Click"></CheckBox>
                                    </DataTemplate>
                                </data:DataGridTemplateColumn.CellTemplate>
                            </data:DataGridTemplateColumn>
                            <data:DataGridTextColumn Binding="{Binding DrugName}"  Localization:DataGridColumnHelper.HeaderBinding="{Binding lblDrug,Source={StaticResource locResource}}" IsReadOnly="True" ></data:DataGridTextColumn>
                            <data:DataGridTemplateColumn Localization:DataGridColumnHelper.HeaderBinding="{Binding lblDrugRate,Source={StaticResource locResource}}" Visibility="Collapsed">
                                <data:DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox x:Name="txtDrugRate" TextAlignment="Right"  IsReadOnly="True" Text="{Binding Rate, Mode=TwoWay}"></TextBox>
                                    </DataTemplate>
                                </data:DataGridTemplateColumn.CellTemplate>
                            </data:DataGridTemplateColumn>

                            <data:DataGridTextColumn    Binding="{Binding Days}" Localization:DataGridColumnHelper.HeaderBinding="{Binding lblDays,Source={StaticResource locResource}}"  IsReadOnly="True" />
                            <data:DataGridTextColumn Visibility="Collapsed"  Binding="{Binding Frequency}" Localization:DataGridColumnHelper.HeaderBinding="{Binding lblFrequency,Source={StaticResource locResource}}" IsReadOnly="True" />


                            <data:DataGridTextColumn Visibility="Collapsed"  Binding="{Binding Quantity}" Localization:DataGridColumnHelper.HeaderBinding="{Binding lblQuantity,Source={StaticResource locResource}}" IsReadOnly="True" />

                            <data:DataGridTextColumn Visibility="Collapsed"  Binding="{Binding Instruction}" Localization:DataGridColumnHelper.HeaderBinding="{Binding lblInstruction,Source={StaticResource locResource}}" IsReadOnly="True" />
                        </data:DataGrid.Columns>
                    </data:DataGrid>
                </controlsToolkit:DockPanel>
            </controlsToolkit:DockPanel>
        </controlsToolkit:DockPanel>
    </Grid>
</controls:ChildWindow>