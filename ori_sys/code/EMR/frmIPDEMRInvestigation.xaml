<UserControl x:Class="EMR.frmIPDEMRInvestigation"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
           xmlns:controls="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls"
           xmlns:inputcombo="clr-namespace:PalashDynamics.Controls;assembly=PalashDynamics.Controls.AutoCompleteComboBox"
           xmlns:input="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Input"
           xmlns:controlsToolkit="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Toolkit"  
           xmlns:data="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Data"
           xmlns:Converter="clr-namespace:PalashDynamics.Converters;assembly=PalashDynamics.Converters"
           xmlns:toolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit"
           xmlns:Localization="clr-namespace:PalashDynamic.Localization;assembly=PalashDynamic.Localization"
           xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/PalashDynamics;component/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Localization:Localized x:Key="locResource"></Localization:Localized>
            <Converter:ToggleBooleanValueConverter x:Key="NegateValueConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid x:Name="LayoutRoot" Background="White">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Grid Margin="4" Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition ></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Grid.Column="0" BorderBrush="{StaticResource TabControlDarkerBorder}"   BorderThickness="1" CornerRadius="3,3,0,0" Background="Transparent">
                    <ContentControl VerticalAlignment="Center" FontSize="12" Content="Investigations" Margin="-1" Style="{StaticResource Header}" HorizontalAlignment="Stretch" />
                </Border>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Margin="4">
                    <ToggleButton   Margin="5,0,10,0" Content="{Binding btnLab,Source={StaticResource locResource}}" Style="{StaticResource ToggleButtonStyle}" x:Name="cmdLab" Click="cmdLab_Click" Width="auto" ></ToggleButton>
                    <ToggleButton  Margin="5,0,10,0" Content="{Binding btnRadiology,Source={StaticResource locResource}}" Style="{StaticResource ToggleButtonStyle}" x:Name="cmdRadiology" Click="cmdRadiology_Click"  Width="auto" ></ToggleButton>
                    <!--<ToggleButton  Margin="5,0,10,0" Content="{Binding btnDiagnostik,Source={StaticResource locResource}}" Style="{StaticResource ToggleButtonStyle}" x:Name="cmdDiagnostik" Click="cmdDiagnostik_Click"  Width="auto" ></ToggleButton>-->
                </StackPanel>

                <ScrollViewer Grid.Row="2"  Background="Transparent" VerticalScrollBarVisibility="Auto" toolkit:DockPanel.Dock="Top"   VerticalAlignment="Stretch" toolkit:ScrollViewerExtensions.IsMouseWheelScrollingEnabled="True">
                    <data:DataGrid HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Grid.ColumnSpan="2" x:Name="dgServiceList" AutoGenerateColumns="False" ItemsSource="{Binding}" IsReadOnly="True" Margin="0"  TabIndex="2"  CanUserSortColumns="True" >
                        <!--<data:DataGrid.RowGroupHeaderStyles>
                            <Style TargetType="data:DataGridRowGroupHeader">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="data:DataGridRowGroupHeader">
                                            <StackPanel Orientation="Horizontal" Background="LightGray" >
                                                <TextBlock Text="{Binding Name}" Margin="4" FontWeight="Bold" x:Name="lblPriority" />
                                                <TextBlock Text="{Binding lblPriority, Source={StaticResource locResource}}" Margin="4"  VerticalAlignment="Center" x:Name="lblGrpPriority"  />
                                                <inputcombo:AutoCompleteComboBox MinWidth="100" x:Name="cmbPriorityGrp" SelectedValue="{Binding SelectedPriority.ID}" SelectedItem="{Binding SelectedPriority,Mode=TwoWay}" ItemsSource="{Binding Priority}" Margin="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Loaded="cmbPriorityGrp_Loaded" SelectionChanged="cmbPriorityGrp_SelectionChanged" >
                                                </inputcombo:AutoCompleteComboBox>
                                            </StackPanel>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Background" Value="#44225566" />
                                <Setter Property="Height" Value="30" />
                            </Style>
                        </data:DataGrid.RowGroupHeaderStyles>-->
                        <data:DataGrid.Columns>
                            <data:DataGridTextColumn Localization:DataGridColumnHelper.HeaderBinding="{Binding lblServiceCode,Source={StaticResource locResource}}" Binding="{Binding ServiceCode}" CanUserResize="False"></data:DataGridTextColumn>
                            <data:DataGridTextColumn Localization:DataGridColumnHelper.HeaderBinding="{Binding lblServiceName,Source={StaticResource locResource}}" Width="auto" Binding="{Binding ServiceName}" ></data:DataGridTextColumn>
                            <!--<data:DataGridTextColumn Localization:DataGridColumnHelper.HeaderBinding="{Binding lblServiceRate,Source={StaticResource locResource}}" Binding="{Binding ServiceRate}" CanUserReorder="True" CanUserResize="False"></data:DataGridTextColumn>-->
                            <data:DataGridTemplateColumn Localization:DataGridColumnHelper.HeaderBinding="{Binding lblPriority,Source={StaticResource locResource}}" MinWidth="100" >
                                <data:DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <inputcombo:AutoCompleteComboBox x:Name="cmbPriority" SelectedValue="{Binding SelectedPriority.ID}" SelectedItem="{Binding SelectedPriority,Mode=TwoWay}" ItemsSource="{Binding Priority}" Margin="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
                                        </inputcombo:AutoCompleteComboBox>
                                    </DataTemplate>
                                </data:DataGridTemplateColumn.CellEditingTemplate>
                            </data:DataGridTemplateColumn>
                            <data:DataGridTemplateColumn>
                                <data:DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <HyperlinkButton VerticalAlignment="Center" FontFamily="Webdings" Content="r"  Padding="5,0,5,0" IsTabStop="False" x:Name="cmdDeleteInvest" ToolTipService.ToolTip="Delete" Click="cmdDeleteInvest_Click" ></HyperlinkButton>
                                    </DataTemplate>
                                </data:DataGridTemplateColumn.CellTemplate>
                            </data:DataGridTemplateColumn>
                        </data:DataGrid.Columns>
                    </data:DataGrid>
                </ScrollViewer>

                <Border VerticalAlignment="Bottom" Grid.Row="3"  BorderBrush="{StaticResource TabControlDarkerBorder}"  Margin="4" toolkit:DockPanel.Dock="Bottom" BorderThickness="1" CornerRadius="3,3,3,3" Background="Transparent" >
                    <StackPanel x:Name="Footer" Orientation="Horizontal" VerticalAlignment="Center"   Margin="0,3,5,3" HorizontalAlignment="Right" Background="Transparent">
                        <StackPanel Orientation="Horizontal" x:Name="spSpecDoctor" Visibility="Collapsed">
                            <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center"  Margin="4,0,4,0" Text="{Binding lblSpecialization, Source={StaticResource locResource}}"  Grid.Row="0" Grid.Column="4"/>
                            <inputcombo:AutoCompleteComboBox  x:Name="cmbSpecialization" Margin="4,0,4,0" VerticalAlignment="Center" ItemsSource="{Binding}" HorizontalAlignment="Stretch" SelectedValuePath="Code" SelectedItemBinding="{Binding MasterListItem, Mode=OneWay}"  MinWidth="70" ></inputcombo:AutoCompleteComboBox>
                            <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center"  Margin="4,0,4,0" Text="{Binding lblDoctor, Source={StaticResource locResource}}"  Grid.Row="0" Grid.Column="4"/>
                            <inputcombo:AutoCompleteComboBox  x:Name="cmbDoctor" Margin="4,0,4,0" VerticalAlignment="Center" ItemsSource="{Binding}" HorizontalAlignment="Stretch" SelectedValuePath="Code" SelectedItemBinding="{Binding MasterListItem, Mode=OneWay}" TabIndex="2" MinWidth="70" ></inputcombo:AutoCompleteComboBox>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal"  Margin="1" HorizontalAlignment="Right"  >

                            <ToggleButton   Margin="5,0,10,0" Content="{Binding btnSave,Source={StaticResource locResource}}" Style="{StaticResource ToggleButtonStyle}" x:Name="cmdSave" Click="cmdSave_Click" Width="auto"></ToggleButton>

                            <ToggleButton  Margin="5,0,0,0" Content="{Binding btnCancel,Source={StaticResource locResource}}" Style="{StaticResource ToggleButtonStyle}" x:Name="cmdCancel" Click="cmdCancel_Click"  Width="auto" ></ToggleButton>

                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>

            <toolkit:DockPanel Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="4">
                <Border controlsToolkit:DockPanel.Dock="Top" BorderBrush="{StaticResource TabControlDarkerBorder}" BorderThickness="1" CornerRadius="3,3,0,0" Background="Transparent">
                    <ContentControl VerticalAlignment="Center" FontSize="12" Content="{Binding lblPreviousInvestigations,Source={StaticResource locResource}}" Margin="-1" Style="{StaticResource Header}" HorizontalAlignment="Stretch" />
                </Border>
                <data:DataPager x:Name="pgrPrevInvestigations" AutoEllipsis="False" PageSize="{Binding PageSize}" DisplayMode="FirstLastPreviousNext" IsEnabled="True" IsTotalItemCountFixed="True" toolkit:DockPanel.Dock="Bottom"/>

                <ScrollViewer Background="Transparent" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" toolkit:DockPanel.Dock="Top"   VerticalAlignment="Stretch" toolkit:ScrollViewerExtensions.IsMouseWheelScrollingEnabled="True">
                    <data:DataGrid HorizontalAlignment="Stretch" x:Name="dgPastVisitServices" AutoGenerateColumns="False" ItemsSource="{Binding}" VerticalScrollBarVisibility="Visible" IsReadOnly="True" TabIndex="2"  CanUserSortColumns="True" >
                        <data:DataGrid.Columns>
                            <data:DataGridTextColumn Localization:DataGridColumnHelper.HeaderBinding="{Binding lblServiceCode,Source={StaticResource locResource}}" Binding="{Binding ServiceCode }"></data:DataGridTextColumn>
                            <data:DataGridTextColumn Localization:DataGridColumnHelper.HeaderBinding="{Binding lblServiceName,Source={StaticResource locResource}}" Binding="{Binding ServiceName}" Width="300"></data:DataGridTextColumn>
                            <!--<data:DataGridTextColumn Localization:DataGridColumnHelper.HeaderBinding="{Binding lblServiceRate,Source={StaticResource locResource}}" Binding="{Binding Rate}"></data:DataGridTextColumn>-->
                            <data:DataGridTextColumn Localization:DataGridColumnHelper.HeaderBinding="{Binding lblPriority,Source={StaticResource locResource}}" Binding="{Binding SelectedPriority.Description}"></data:DataGridTextColumn>
                        </data:DataGrid.Columns>
                    </data:DataGrid>
                </ScrollViewer>

            </toolkit:DockPanel>
        </Grid>
    </Grid>
</UserControl>
