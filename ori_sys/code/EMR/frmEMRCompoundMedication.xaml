<UserControl x:Class="EMR.frmEMRCompoundMedication"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:controls="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls"
    xmlns:inputcombo="clr-namespace:PalashDynamics.Controls;assembly=PalashDynamics.Controls.AutoCompleteComboBox"
    xmlns:input="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Input" 
    xmlns:Localization="clr-namespace:PalashDynamic.Localization;assembly=PalashDynamic.Localization"
    xmlns:data="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Data"
    xmlns:Converter="clr-namespace:PalashDynamics.Converters;assembly=PalashDynamics.Converters"
    xmlns:toolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/PalashDynamics;component/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Localization:Localized x:Key="locResource"></Localization:Localized>
            <Converter:ToggleBooleanValueConverter x:Key="NegateValueConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.6*"></ColumnDefinition>
            <ColumnDefinition Width="0.4*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="4" Grid.Column="0">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition ></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Grid.Column="0" BorderBrush="{StaticResource TabControlDarkerBorder}"   BorderThickness="1" CornerRadius="3,3,0,0" Background="Transparent">
                <ContentControl VerticalAlignment="Center" FontSize="12" Content="{Binding lblCurrentPrescriptions,Source={StaticResource locResource}}" x:Name="lblCurrentMedication" Margin="-1" Style="{StaticResource Header}"     HorizontalAlignment="Stretch" />
            </Border>
            <data:DataGrid  Grid.Row="2"  Grid.ColumnSpan="4" x:Name="dgDrugList" Margin="2"  AutoGenerateColumns="False" ItemsSource="{Binding}"    HorizontalAlignment="Stretch" TabIndex="6" HorizontalScrollBarVisibility="Auto"  VerticalScrollBarVisibility="Auto" toolkit:DockPanel.Dock="Top"   VerticalAlignment="Stretch" >
                <data:DataGrid.RowGroupHeaderStyles  >
                    <!-- Style for groups at top level -->
                    <Style TargetType="data:DataGridRowGroupHeader" >
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="data:DataGridRowGroupHeader" >
                                    <StackPanel Orientation="Horizontal" Background="LightGray" >
                                        <TextBlock Text="{Binding Name}" Width="120" Margin="4" FontWeight="Bold"  VerticalAlignment="Center" Grid.Column="0"/>
                                        <TextBlock Text="{Binding lblQuantity, Source={StaticResource locResource}}" Margin="4" VerticalAlignment="Center" />
                                        <TextBox x:Name="txtGrpQuantity"  VerticalAlignment="Center" Text="{Binding sCompoundQuantity,Mode=TwoWay}" Width="120"  MaxLength="50" LostFocus="TextBox_LostFocus" Loaded="TextBox_Loaded" Grid.Column="2" ></TextBox>
                                        <TextBlock Text="{Binding lblFrequency, Source={StaticResource locResource}}" Margin="4"  VerticalAlignment="Center"/>
                                        <TextBox x:Name="txtGrpFrequency" VerticalAlignment="Center" Margin="4" Grid.Column="4" Width="100" MaxLength="200" Height="25" LostFocus="TextBox_LostFocus" Loaded="TextBox_Loaded" ></TextBox>
                                        <TextBlock Text="Instruction" Margin="4"  VerticalAlignment="Center"/>
                                        <TextBox x:Name="txtGrpInstruction" VerticalAlignment="Center" Margin="4" Width="120"  Height="25" Grid.Column="2" LostFocus="TextBox_LostFocus" Loaded="TextBox_Loaded"></TextBox>
                                    </StackPanel>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Background" Value="#44225566" />
                        <Setter Property="Height" Value="30" />
                    </Style>
                </data:DataGrid.RowGroupHeaderStyles>
                <data:DataGrid.Columns>
                    <data:DataGridTextColumn Binding="{Binding CompoundDrug}" IsReadOnly="True"  Localization:DataGridColumnHelper.HeaderBinding="{Binding lblCompoundDrug, Source={StaticResource locResource}}" ></data:DataGridTextColumn>
                    <data:DataGridTextColumn Binding="{Binding DrugName}" IsReadOnly="True"  Localization:DataGridColumnHelper.HeaderBinding="{Binding lblDrug, Source={StaticResource locResource}}" ></data:DataGridTextColumn>
                    <data:DataGridTextColumn  Binding="{Binding sComponentQuantity, Mode=TwoWay}" Localization:DataGridColumnHelper.HeaderBinding="{Binding lblDose,Source={StaticResource locResource}}" />
                    <data:DataGridTextColumn  Binding="{Binding AvailableStock}" Localization:DataGridColumnHelper.HeaderBinding="{Binding lblAvailableStock,Source={StaticResource locResource}}" IsReadOnly="True" />
                    <data:DataGridTemplateColumn>
                        <data:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <HyperlinkButton VerticalAlignment="Center" FontFamily="Webdings" Content="r"  Padding="5,0,5,0" IsTabStop="False" x:Name="cmdDelete" ToolTipService.ToolTip="Delete" Click="cmdDeleteDrug_Click"></HyperlinkButton>
                            </DataTemplate>
                        </data:DataGridTemplateColumn.CellTemplate>
                    </data:DataGridTemplateColumn>
                </data:DataGrid.Columns>
            </data:DataGrid>
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Margin="0,2,0,2">
                <ToggleButton x:Name="btnCompdDrug" Content="{Binding btnCompdDrug,Source={StaticResource locResource}}" Grid.Row="0" Grid.Column="1" Click="btnCompdDrug_Click"  Background="#000000" Margin="5,0,0,0" HorizontalAlignment="Left" Style="{StaticResource ToggleButtonStyle}" Visibility="Visible"></ToggleButton>
            </StackPanel>
            <Border VerticalAlignment="Bottom" Grid.Row="3"  BorderBrush="{StaticResource TabControlDarkerBorder}"  Margin="4" toolkit:DockPanel.Dock="Bottom" BorderThickness="1" CornerRadius="3,3,3,3" Background="Transparent" >
                <StackPanel x:Name="Footer" Orientation="Horizontal" VerticalAlignment="Center"   Margin="0,3,5,3" HorizontalAlignment="Right" Background="Transparent">
                    <StackPanel Orientation="Horizontal" x:Name="spSpecDoctor" Visibility="Collapsed">
                        <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center"  Margin="4,0,4,0" Text="{Binding lblSpecialization, Source={StaticResource locResource}}"  Grid.Row="0" Grid.Column="4"/>
                        <inputcombo:AutoCompleteComboBox  x:Name="cmbSpecialization" Margin="4,0,4,0" VerticalAlignment="Center" ItemsSource="{Binding}" HorizontalAlignment="Stretch" SelectedValuePath="Code" SelectedItemBinding="{Binding MasterListItem, Mode=OneWay}"  MinWidth="70" ></inputcombo:AutoCompleteComboBox>
                        <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center"  Margin="4,0,4,0" Text="{Binding lblDoctor, Source={StaticResource locResource}}"  Grid.Row="0" Grid.Column="4"/>
                        <inputcombo:AutoCompleteComboBox  x:Name="cmbDoctor" Margin="4,0,4,0" VerticalAlignment="Center" ItemsSource="{Binding}" HorizontalAlignment="Stretch" SelectedValuePath="Code" SelectedItemBinding="{Binding MasterListItem, Mode=OneWay}" TabIndex="2" MinWidth="70" ></inputcombo:AutoCompleteComboBox>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal"  Margin="1" HorizontalAlignment="Right"  >
                        <ToggleButton   Margin="5,0,10,0" Content="{Binding btnSave,Source={StaticResource locResource}}" Style="{StaticResource ToggleButtonStyle}" x:Name="cmdSave" Click="cmdSave_Click"  Width="auto"></ToggleButton>
                        <ToggleButton  Margin="5,0,5,0" Content="{Binding btnCancel,Source={StaticResource locResource}}" Style="{StaticResource ToggleButtonStyle}" x:Name="cmdCancel" Click="cmdCancel_Click" Width="auto"></ToggleButton>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
        <toolkit:DockPanel Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="4">
            <data:DataPager x:Name="pgrPrevPrescriptions" AutoEllipsis="False" PageSize="{Binding PageSize}" DisplayMode="FirstLastPreviousNext" IsEnabled="True" IsTotalItemCountFixed="True" toolkit:DockPanel.Dock="Bottom"/>
            <Border toolkit:DockPanel.Dock="Top" BorderBrush="{StaticResource TabControlDarkerBorder}"   BorderThickness="1" CornerRadius="3,3,0,0"  Background="Transparent">
                <ContentControl VerticalAlignment="Center" FontSize="12" Content="{Binding lblPreviousPrescriptions,Source={StaticResource locResource}}" Margin="-1" Style="{StaticResource Header}"     HorizontalAlignment="Stretch" />
            </Border>
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <data:DataGrid  x:Name="dgPreviousPrescriptions"  AutoGenerateColumns="False" ItemsSource="{Binding}" HorizontalAlignment="Stretch" TabIndex="6" HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Auto" toolkit:DockPanel.Dock="Top">
                    <data:DataGrid.RowGroupHeaderStyles>
                        <!-- Style for groups at top level -->
                        <Style TargetType="data:DataGridRowGroupHeader" >
                            
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="data:DataGridRowGroupHeader" >
                                        <StackPanel Orientation="Horizontal" Background="LightGray" HorizontalAlignment="Stretch">
                                            <TextBlock Text="{Binding Name}" Width="auto" Margin="4" FontWeight="Bold"  VerticalAlignment="Center" />
                                            <!--<TextBlock Name="CDrug" Text="{Binding CompoundDrug}" Width="auto" Margin="4" FontWeight="Bold"  VerticalAlignment="Center" Loaded="CDrug_Loaded"/>-->
                                            <TextBlock Name="lblQuantity" Text="{Binding lblQuantity, Source={StaticResource locResource}}" Margin="4" VerticalAlignment="Center" Loaded="lblQuantity_Loaded" />
                                            <TextBox x:Name="txtGrpQuantityPrv"  VerticalAlignment="Center" Text="{Binding sComponentQuantity,Mode=OneWay}" Width="auto"  MaxLength="50"  Loaded="TextBox_LoadedPrv" Grid.Column="2" IsReadOnly="True" ></TextBox>
                                            <TextBlock Name="lblFrequency" Text="{Binding lblFrequency, Source={StaticResource locResource}}" Margin="4"  VerticalAlignment="Center" Loaded="lblQuantity_Loaded"/>
                                            <TextBox x:Name="txtGrpFrequencyPrv" Text="{Binding Frequency,Mode=OneWay}"  VerticalAlignment="Center" Margin="4" Width="auto" MaxLength="200" Height="25" Loaded="TextBox_LoadedPrv" IsReadOnly="True"  ></TextBox>
                                            <TextBlock Name="lblInstruction" Text="Instruction" Margin="4"  VerticalAlignment="Center" Loaded="lblQuantity_Loaded"/>
                                            <TextBox x:Name="txtGrpInstructionPrv" Text="{Binding Instruction,Mode=OneWay}"  VerticalAlignment="Center" Margin="4" Width="120"  Height="25" Grid.Column="2"  Loaded="TextBox_LoadedPrv" IsReadOnly="True" ></TextBox>
                                        </StackPanel>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Background" Value="#44225566" />
                            <Setter Property="Width" Value="auto"/>
                            
                            <Setter Property="HorizontalAlignment" Value="Stretch"></Setter>
                            
                            <Setter Property="Height" Value="30" />
                        </Style>
                        
                    </data:DataGrid.RowGroupHeaderStyles>
                    <data:DataGrid.Columns>
                        <data:DataGridTextColumn Binding="{Binding DrugName}" IsReadOnly="True" Localization:DataGridColumnHelper.HeaderBinding="{Binding lblDrug,Source={StaticResource locResource}}" ></data:DataGridTextColumn>
                        <!--<data:DataGridTemplateColumn Localization:DataGridColumnHelper.HeaderBinding="{Binding lblFrequency,Source={StaticResource locResource}}" IsReadOnly="True" >
                            <data:DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <inputcombo:AutoCompleteComboBox x:Name="cmbFrequency"   SelectedValue="{Binding SelectedFrequency.ID}" SelectedItem="{Binding Frequency,Mode=TwoWay}" ItemsSource="{Binding Frequency}" Margin="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" SelectionChanged="cmbFrequency_SelectionChanged" IsEnabled="False" ></inputcombo:AutoCompleteComboBox>
                                </DataTemplate>
                            </data:DataGridTemplateColumn.CellEditingTemplate>
                        </data:DataGridTemplateColumn>-->
                        <data:DataGridTextColumn  Binding="{Binding sComponentQuantity}" Localization:DataGridColumnHelper.HeaderBinding="{Binding lblDose,Source={StaticResource locResource}}" IsReadOnly="True" />
                        <!--<data:DataGridTemplateColumn Localization:DataGridColumnHelper.HeaderBinding="{Binding lblInstruction,Source={StaticResource locResource}}" IsReadOnly="True" >
                            <data:DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <inputcombo:AutoCompleteComboBox x:Name="cmbInstruction"  SelectedValue="{Binding SelectedInstruction.ID}" SelectedItem="{Binding Instruction,Mode=TwoWay}" ItemsSource="{Binding Instruction}" Margin="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" IsEnabled="False" >
                                    </inputcombo:AutoCompleteComboBox>
                                </DataTemplate>
                            </data:DataGridTemplateColumn.CellEditingTemplate>
                        </data:DataGridTemplateColumn>-->
                        <!--<data:DataGridTemplateColumn>
                            <data:DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <HyperlinkButton VerticalAlignment="Center" FontFamily="Webdings" Content="r"  Padding="5,0,5,0" IsTabStop="False" x:Name="cmdDelete" ToolTipService.ToolTip="Delete" Click="cmdDeleteDrug_Click"></HyperlinkButton>
                                </DataTemplate>
                            </data:DataGridTemplateColumn.CellTemplate>
                        </data:DataGridTemplateColumn>-->
                    </data:DataGrid.Columns>
                </data:DataGrid>
            </ScrollViewer>
        </toolkit:DockPanel>
    </Grid>
</UserControl>