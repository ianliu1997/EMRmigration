<controls:ChildWindow x:Class="EMR.CompoundDrugList"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
xmlns:data="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Data"
xmlns:controlsToolkit="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Toolkit" 
xmlns:inputcombo="clr-namespace:PalashDynamics.Controls;assembly=PalashDynamics.Controls.AutoCompleteComboBox"
xmlns:controls="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls"
xmlns:Localization="clr-namespace:PalashDynamic.Localization;assembly=PalashDynamic.Localization"
Loaded="ChildWindow_Loaded">

    <controls:ChildWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/PalashDynamic;component/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Localization:Localized x:Key="locResource"></Localization:Localized>
        </ResourceDictionary>
    </controls:ChildWindow.Resources>

    <Grid x:Name="LayoutRoot" Margin="2" Background="White">

        <controlsToolkit:DockPanel x:Name="Main"  >
            <controlsToolkit:DockPanel x:Name="docFooter" controlsToolkit:DockPanel.Dock="Bottom" >
                <TextBlock  Margin="4" Text="{Binding lblGiveNameCompoundDrug, Source={StaticResource locResource}}"></TextBlock>
                <TextBox x:Name="txtCName" Margin="4"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" MinWidth="220" MinHeight="25" MaxLength="255"></TextBox>
                <StackPanel x:Name="Footer" Orientation="Horizontal" VerticalAlignment="Center"   Margin="0,3,5,3" HorizontalAlignment="Right" Background="Transparent">
                    <ToggleButton  x:Name="cmdAdd" Grid.Row="4" Content="{Binding btnAdd, Source={StaticResource locResource}}" Margin="5,0,5,0" Style="{StaticResource ToggleButtonStyle}" Click="OKButton_Click" TabIndex="7" Height="21" />
                    <ToggleButton  x:Name="cmdClose" Grid.Row="4" Content="{Binding btnCancel, Source={StaticResource locResource}}" Margin="5,0,0,0" Style="{StaticResource ToggleButtonStyle}" Click="CancelButton_Click" Height="21" TabIndex="8"/>
                </StackPanel>
            </controlsToolkit:DockPanel>

            <controlsToolkit:DockPanel x:Name="DockTop" controlsToolkit:DockPanel.Dock="Top" HorizontalAlignment="Stretch">
                <Grid x:Name="LayoutRoot2" Background="White" Margin="4" HorizontalAlignment="Stretch"  >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"></ColumnDefinition>
                        <ColumnDefinition ></ColumnDefinition>
                        <ColumnDefinition Width="Auto"></ColumnDefinition>
                        <ColumnDefinition ></ColumnDefinition>
                    
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"></RowDefinition>
                        <RowDefinition Height="auto"></RowDefinition>
                    </Grid.RowDefinitions>

                    <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center" Text="{Binding lblBrandName, Source={StaticResource locResource}}" Margin="4" />
                    <TextBox x:Name="txtBrandName" Grid.Column="1" Margin="4" Text="{Binding Description, Mode=TwoWay}" HorizontalAlignment="Stretch"  MaxLength="50" Height="25" TabIndex="1" KeyDown="txtServiceName_KeyDown" ></TextBox>

                    <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center" Text="{Binding lblMolecule, Source={StaticResource locResource}}" Grid.Column="2" Margin="4" />
                    <inputcombo:AutoCompleteComboBox x:Name="cmbMolecule" SelectedValuePath="Code" SelectedItemBinding="{Binding MasterListItem, Mode=OneWay}"  Grid.Row="0" Grid.Column="3" Height="25"  Margin="4"  HorizontalAlignment="Stretch" TabIndex="2" SelectionChanged="cmbMolecule_SelectionChanged"/>

                    <Button x:Name="cmdSearch" Click="cmdSearch_Click" Margin="4" ToolTipService.ToolTip="{Binding ttpSearchDoctor, Source={StaticResource locResource}}" Grid.Column="3" TabIndex="3" Grid.Row="1" Width="auto" HorizontalAlignment="Left">
                        <Image Source="/PalashDynamics;component/Icons/Search_b.JPG" Margin="4,0,4,4" Width="20" ></Image>
                    </Button>

                    <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center" Text="{Binding lblCompoundDrug, Source={StaticResource locResource}}" Margin="4" Grid.Row="1" />
                    <TextBox x:Name="txtCompoundName" Grid.Column="1" Grid.Row="1" Margin="4" Text="{Binding Description, Mode=TwoWay}" HorizontalAlignment="Stretch"  MaxLength="50" Height="25" TabIndex="1" KeyDown="txtServiceName_KeyDown" ></TextBox>
                </Grid>
            </controlsToolkit:DockPanel>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>

                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <controlsToolkit:DockPanel x:Name="serviceOrderList1" LastChildFill="True" Grid.Row="0" Margin="4" >
                    <StackPanel controlsToolkit:DockPanel.Dock="Top">
                        <Border  BorderBrush="{StaticResource TabControlDarkerBorder}"   BorderThickness="1" CornerRadius="3,3,0,0"  Background="Transparent">
                            <ContentControl VerticalAlignment="Center" FontSize="12" Content="{Binding lblListOfDrug, Source={StaticResource locResource}}" Margin="-1" Style="{StaticResource Header}"     HorizontalAlignment="Stretch" />
                        </Border>
                    </StackPanel>

                    <data:DataPager x:Name="DataPagerDoc" controlsToolkit:DockPanel.Dock="Bottom" 
                                                        DisplayMode="FirstLastPreviousNext"  IsEnabled="True"                       
                                                        Source="{Binding}" 
                                                        AutoEllipsis="False" IsTotalItemCountFixed="False"  />

                    <data:DataGrid controlsToolkit:DockPanel.Dock="Top"  x:Name="dgDrugList" AutoGenerateColumns="False" ItemsSource="{Binding}" VerticalScrollBarVisibility="Visible" IsReadOnly="False" Margin="0,0,0,0" HorizontalAlignment="Stretch" TabIndex="4" Width="Auto" VerticalAlignment="Stretch">
                        <data:DataGrid.Columns>
                            <data:DataGridTemplateColumn>
                                <data:DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox x:Name="chkMultipleServices" IsChecked="{Binding Selected, Mode=TwoWay}" Click="chkMultipleDrug_Click"></CheckBox>
                                    </DataTemplate>
                                </data:DataGridTemplateColumn.CellTemplate>
                            </data:DataGridTemplateColumn>
                            <data:DataGridTextColumn IsReadOnly="True" Localization:DataGridColumnHelper.HeaderBinding="{Binding lblDrug, Source={StaticResource locResource}}" Width="Auto"  Binding="{Binding Description, Mode=TwoWay}" />
                            <data:DataGridTextColumn IsReadOnly="True" Localization:DataGridColumnHelper.HeaderBinding="{Binding lblAvailableStock, Source={StaticResource locResource}}" Width="Auto"  Binding="{Binding AvailableStock, Mode=TwoWay}" />
                        </data:DataGrid.Columns>
                    </data:DataGrid>

                </controlsToolkit:DockPanel>

                <controlsToolkit:DockPanel x:Name="serviceOrderList2" LastChildFill="True" Grid.Row="0" Grid.Column="1" Margin="4" >

                    <StackPanel controlsToolkit:DockPanel.Dock="Top">
                        <Border  BorderBrush="{StaticResource TabControlDarkerBorder}"   BorderThickness="1" CornerRadius="3,3,0,0"  Background="Transparent">
                            <ContentControl VerticalAlignment="Center" FontSize="12" Content="{Binding lblListOfCompoundDrug, Source={StaticResource locResource}}" Margin="-1" Style="{StaticResource Header}"     HorizontalAlignment="Stretch" />
                        </Border>
                    </StackPanel>

                    <data:DataGrid controlsToolkit:DockPanel.Dock="Top"  x:Name="dgComList" AutoGenerateColumns="False" ItemsSource="{Binding}" VerticalScrollBarVisibility="Visible" IsReadOnly="False" Margin="0,0,0,0" HorizontalAlignment="Stretch" TabIndex="2" Width="Auto" VerticalAlignment="Stretch">
                        <data:DataGrid.RowGroupHeaderStyles>
                            <!-- Style for groups at top level -->
                            <Style TargetType="data:DataGridRowGroupHeader">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="data:DataGridRowGroupHeader">
                                            <StackPanel Orientation="Horizontal" Background="LightGray" Height="30" >
                                                <TextBlock Text="{Binding Name}" Margin="4" FontWeight="Bold" />
                                                <CheckBox Margin="4" Click="HyperlinkButton_Click" Foreground="Black" FontWeight="Bold" />
                                            </StackPanel>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Background" Value="#44225566" />

                            </Style>
                        </data:DataGrid.RowGroupHeaderStyles>
                        <data:DataGrid.Columns>
                            <data:DataGridTextColumn IsReadOnly="True" Localization:DataGridColumnHelper.HeaderBinding="{Binding lblCompoundDrug, Source={StaticResource locResource}}" Width="Auto"  Binding="{Binding CompoundDrug, Mode=TwoWay}" />
                            <data:DataGridTextColumn IsReadOnly="True" Localization:DataGridColumnHelper.HeaderBinding="{Binding lblDrug, Source={StaticResource locResource}}" Width="Auto"  Binding="{Binding ItemName, Mode=TwoWay}" />
                            <data:DataGridTextColumn IsReadOnly="True" Localization:DataGridColumnHelper.HeaderBinding="{Binding lblAvailableStock, Source={StaticResource locResource}}" Width="Auto"  Binding="{Binding AvailableStock, Mode=TwoWay}" />
                        </data:DataGrid.Columns>
                    </data:DataGrid>
                </controlsToolkit:DockPanel>

                <controlsToolkit:DockPanel x:Name="serviceOrderList3" LastChildFill="True" Grid.Row="1" Grid.ColumnSpan="2">

                    <StackPanel controlsToolkit:DockPanel.Dock="Top">
                        <Border  BorderBrush="{StaticResource TabControlDarkerBorder}"   BorderThickness="1" CornerRadius="3,3,0,0"  Background="Transparent">
                            <ContentControl VerticalAlignment="Center" FontSize="12" Content="{Binding lblSelectedDrug, Source={StaticResource locResource}}" Margin="-1" Style="{StaticResource Header}" HorizontalAlignment="Stretch" />
                        </Border>
                    </StackPanel>
                    <data:DataGrid controlsToolkit:DockPanel.Dock="Top"  x:Name="dgSelectedList" AutoGenerateColumns="False" ItemsSource="{Binding}" VerticalScrollBarVisibility="Visible" IsReadOnly="False" Margin="0,0,0,0" HorizontalAlignment="Stretch" TabIndex="2" Width="Auto" VerticalAlignment="Stretch">
                        <data:DataGrid.Columns>
                            <data:DataGridTextColumn IsReadOnly="True" Localization:DataGridColumnHelper.HeaderBinding="{Binding lblDrug, Source={StaticResource locResource}}" Width="Auto"  Binding="{Binding Description, Mode=TwoWay}" />
                            <data:DataGridTextColumn IsReadOnly="True" Localization:DataGridColumnHelper.HeaderBinding="{Binding lblAvailableStock, Source={StaticResource locResource}}" Width="Auto"  Binding="{Binding StockQty, Mode=TwoWay}" Visibility="Collapsed" />
                            <data:DataGridTextColumn IsReadOnly="True" Localization:DataGridColumnHelper.HeaderBinding="{Binding lblCompoundDrug, Source={StaticResource locResource}}" Width="Auto"  Binding="{Binding PrintName, Mode=TwoWay}" />
                        </data:DataGrid.Columns>
                    </data:DataGrid>
                </controlsToolkit:DockPanel>
            </Grid>
        </controlsToolkit:DockPanel>
    </Grid>
</controls:ChildWindow>