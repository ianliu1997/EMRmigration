<UserControl x:Class="PalashDynamics.Administration.frmDoctorShareRange"
     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit"
    xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk"
    xmlns:controls="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls"
    xmlns:GroupBox="clr-namespace:Groupbox;assembly=Groupbox"  
    xmlns:controlsToolkit="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Toolkit"
    xmlns:inputcombo="clr-namespace:PalashDynamics.Controls;assembly=PalashDynamics.Controls.AutoCompleteComboBox"
    mc:Ignorable="d" Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/PalashDynamics;component/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid x:Name="LayoutRoot" Background="White">
        <toolkit:DockPanel LastChildFill="True" x:Name="MainDockPanel" >
            <Border  BorderBrush="{StaticResource TabControlDarkerBorder}"  Margin="4" toolkit:DockPanel.Dock="Bottom" BorderThickness="1" CornerRadius="3,3,3,3" Background="Transparent">
                <StackPanel x:Name="Footer" Orientation="Horizontal" VerticalAlignment="Center"   Margin="0,3,5,3" HorizontalAlignment="Right" Background="Transparent">
                    <StackPanel Orientation="Horizontal"  Margin="1" HorizontalAlignment="Right"  >
                        <ToggleButton  Margin="4" Content="New" Style="{StaticResource ToggleButtonStyle}" x:Name="cmdAdd" Click="cmdAdd_Click"></ToggleButton>
                        <ToggleButton   Margin="4" Content="Save" Style="{StaticResource ToggleButtonStyle}" x:Name="cmdSave" Click="cmdSave_Click"></ToggleButton>
                        <ToggleButton  Margin="4" Content="Cancel" Style="{StaticResource ToggleButtonStyle}" x:Name="cmdCancel" Click="cmdCancel_Click"></ToggleButton>
                    </StackPanel>
                </StackPanel>
            </Border>
            <Grid x:Name="PageContainer" >
                <Grid Height="AUTO" Visibility="Visible" Width="auto" x:Name="FrontPanel" Grid.Row="0">
                    <toolkit:DockPanel x:Name="docList">
                        <toolkit:DockPanel LastChildFill="True">
                            <sdk:DataPager x:Name="dataGrid2Pager" toolkit:DockPanel.Dock="Bottom" Source="{Binding}" NumericButtonCount="10" AutoEllipsis="False" IsTotalItemCountFixed="True"   />
                            <Border  controlsToolkit:DockPanel.Dock="Top" BorderBrush="{StaticResource TabControlDarkerBorder}"   BorderThickness="1" CornerRadius="3,3,0,0" Background="Transparent">
                                <ContentControl VerticalAlignment="Center" FontSize="12" Content="Doctor Share Range Master" Margin="-1" Style="{StaticResource Header}"     HorizontalAlignment="Stretch" />
                            </Border>
                            <sdk:DataGrid toolkit:DockPanel.Dock="Top" x:Name="grdMaster" VerticalAlignment="Stretch" ItemsSource="{Binding}" AutoGenerateColumns="False">
                                <sdk:DataGrid.Columns>
                                    <sdk:DataGridTemplateColumn Header="View">
                                        <sdk:DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <HyperlinkButton Margin="4" x:Name="cmdView" Content="View" Click="cmdView_Click" Cursor="Hand"></HyperlinkButton>
                                            </DataTemplate>
                                        </sdk:DataGridTemplateColumn.CellTemplate>
                                    </sdk:DataGridTemplateColumn>
                                    <sdk:DataGridTextColumn IsReadOnly="True" Header="Lower Limit" Binding="{Binding LowerLimit, StringFormat=0.000}" Width="Auto" />
                                    <sdk:DataGridTextColumn IsReadOnly="True" Header="Upper Limit" Binding="{Binding UpperLimit, StringFormat=0.000}" Width="Auto" />

                                    <sdk:DataGridTextColumn IsReadOnly="True" Header="Share %" Binding="{Binding SharePercentage,StringFormat=0.000}" Width="Auto" />
                                    <sdk:DataGridTextColumn IsReadOnly="True" Header="Share Amount" Binding="{Binding ShareAmount, StringFormat=0.000}" Width="Auto" />
                                    <sdk:DataGridTemplateColumn Header="Status">
                                        <sdk:DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <CheckBox HorizontalAlignment="Center" Margin="4" IsChecked="{Binding Path=Status}" Click="CheckBox_Click"></CheckBox>
                                            </DataTemplate>
                                        </sdk:DataGridTemplateColumn.CellTemplate>
                                    </sdk:DataGridTemplateColumn>
                                </sdk:DataGrid.Columns>
                            </sdk:DataGrid>
                        </toolkit:DockPanel>
                    </toolkit:DockPanel>

                </Grid>

                <Grid  Height="AUTO" Width="auto" x:Name="BackPanel" Visibility="Collapsed" Grid.Row="1">
                    <toolkit:DockPanel x:Name="Content" toolkit:DockPanel.Dock="top" LastChildFill="true"  >
                        <Border  BorderBrush="{StaticResource TabControlDarkerBorder}"    BorderThickness="1" CornerRadius="3,3,0,0" Margin="4,4,4,4"  Background="Transparent">
                            <toolkit:DockPanel toolkit:DockPanel.Dock="Top">
                                <toolkit:DockPanel x:Name="docHeader" toolkit:DockPanel.Dock="Top" HorizontalAlignment="Stretch">
                                    <Border  BorderBrush="{StaticResource TabControlDarkerBorder}" toolkit:DockPanel.Dock="Top"   Margin="0,-1,0,0" BorderThickness="1" CornerRadius="0,0,3,3" Background="Transparent">
                                        <GroupBox:GroupBox x:Name="grpMasterDetails"  Margin="4" Width="Auto" Height="Auto" Grid.Row="0" HorizontalAlignment="Stretch">
                                            <Grid x:Name="grdMasterBackPanel">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                                                    <ColumnDefinition ></ColumnDefinition>
                                                </Grid.ColumnDefinitions>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>

                                                <TextBlock  Grid.Row="1" Grid.Column="0" Text="Upper Limit" Margin="4"></TextBlock>
                                                <TextBox   Grid.Row="1" Grid.Column="1" x:Name="txtUpperLimit" Text="{Binding UpperLimit,Mode=TwoWay,StringFormat=0.000}" Margin="4"  MaxLength="50" TextChanged="txtNumber_TextChanged" KeyDown="txtNumber_KeyDown"/>

                                                <TextBlock  Grid.Row="0" Grid.Column="0" Text="Lower Limit" Margin="4"></TextBlock>
                                                <TextBox   Grid.Row="0" Grid.Column="1" x:Name="txtLowerLimit" Text="{Binding LowerLimit,Mode=TwoWay,StringFormat=0.000}" Margin="4"  MaxLength="50" TextChanged="txtNumber_TextChanged" KeyDown="txtNumber_KeyDown"/>

                                                <TextBlock  Grid.Row="2" Grid.Column="0" Text="Share %" Margin="4"></TextBlock>
                                                <TextBox   Grid.Row="2" Grid.Column="1" x:Name="txtSharePercentage" Text="{Binding SharePercentage,Mode=TwoWay,StringFormat=0.00}" Margin="4"  MaxLength="50" TextChanged="txtNumber_TextChanged" KeyDown="txtNumber_KeyDown"/>

                                                <TextBlock  Grid.Row="3" Grid.Column="0" Text="Share Amount" Margin="4"></TextBlock>
                                                <TextBox   Grid.Row="3" Grid.Column="1" x:Name="txtShareAmount" Text="{Binding ShareAmount,Mode=TwoWay,StringFormat=0.000}" Margin="4"  MaxLength="50" TextChanged="txtNumber_TextChanged" KeyDown="txtNumber_KeyDown"/>
                                            </Grid>
                                        </GroupBox:GroupBox>

                                    </Border>
                                </toolkit:DockPanel>

                            </toolkit:DockPanel>
                        </Border>

                    </toolkit:DockPanel>
                </Grid>
            </Grid>

        </toolkit:DockPanel>
    </Grid>
</UserControl>
