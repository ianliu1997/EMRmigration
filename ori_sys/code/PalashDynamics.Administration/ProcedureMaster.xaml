<UserControl x:Class="PalashDynamics.Administration.ProcedureMaster"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls"
    xmlns:inputcombo="clr-namespace:PalashDynamics.Controls;assembly=PalashDynamics.Controls.AutoCompleteComboBox" 
    xmlns:controlsToolkit="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Toolkit"  
    xmlns:data="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Data" 
    xmlns:GroupBox="clr-namespace:Groupbox;assembly=Groupbox"   
    mc:Ignorable="d"
    xmlns:Localization="clr-namespace:PalashDynamic.Localization;assembly=PalashDynamic.Localization"
    Height="Auto" Width="Auto" Loaded="UserControl_Loaded">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/PalashDynamics;component/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <ControlTemplate x:Key="FilledContentControlTemplate" TargetType="ContentControl">
                <Grid>
                    <ContentPresenter />
                </Grid>
            </ControlTemplate>
            <Localization:Localized x:Key="locResource"></Localization:Localized>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <controlsToolkit:DockPanel LastChildFill="True">
            <Border  BorderBrush="{StaticResource TabControlDarkerBorder}"  Margin="4" controlsToolkit:DockPanel.Dock="Bottom" BorderThickness="1" CornerRadius="3,3,3,3" Background="Transparent" >
                <StackPanel x:Name="Footer" Orientation="Horizontal" VerticalAlignment="Center"   Margin="0,3,5,3" HorizontalAlignment="Right" Background="Transparent">
                    <StackPanel Orientation="Horizontal"  Margin="1" HorizontalAlignment="Right"  >
                        <ToggleButton  Margin="4"  Content="{Binding btnNew,Source={StaticResource locResource}}" Style="{StaticResource ToggleButtonStyle}" x:Name="cmdAdd" Click="cmdAdd_Click"></ToggleButton>
                        <ToggleButton   Margin="4" Content="{Binding btnSave,Source={StaticResource locResource}}" Style="{StaticResource ToggleButtonStyle}" x:Name="cmdSave" Click="cmdSave_Click"></ToggleButton>
                        <ToggleButton   Margin="4"  Content="{Binding btnModify,Source={StaticResource locResource}}" Style="{StaticResource ToggleButtonStyle}" x:Name="cmdModify" Click="cmdModify_Click"></ToggleButton>
                        <ToggleButton  Margin="4" Content="{Binding btnCancel,Source={StaticResource locResource}}"  Style="{StaticResource ToggleButtonStyle}" x:Name="cmdCancel" Click="cmdCancel_Click"></ToggleButton>
                    </StackPanel>
                </StackPanel>
            </Border>
            <Grid x:Name="PageContainer" >
                <Grid Height="AUTO" Visibility="Visible" Width="auto" x:Name="FrontPanel" Grid.Row="0">
                    <controlsToolkit:DockPanel x:Name="procList">
                        <controlsToolkit:DockPanel x:Name="DockTop" controlsToolkit:DockPanel.Dock="Top" HorizontalAlignment="Stretch">
                            <controlsToolkit:DockPanel LastChildFill="True" controlsToolkit:DockPanel.Dock="Top">
                                <Grid controlsToolkit:DockPanel.Dock="Top">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition></ColumnDefinition>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition></ColumnDefinition>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock x:Name="txtSearchCriteria" Grid.Column="0" Text="{Binding lblDescription,Source={StaticResource locResource}}" Margin="4" Width="75"/>
                                    <TextBox Grid.Column="1" x:Name="txtSearch" Margin="4"  ></TextBox>

                                    <TextBlock  Grid.Column="2" Text="{Binding lblProcedureType,Source={StaticResource locResource}}" Margin="4" />
                                    <inputcombo:AutoCompleteComboBox  Grid.Row="0" Grid.Column="3" Margin="4" x:Name="CmbProcedureTypeSearch"  SelectedItemBinding="{Binding MasterListItem, Mode=TwoWay}" SelectedValuePath="ID"  HorizontalAlignment="Stretch"></inputcombo:AutoCompleteComboBox >
                                    <Button Grid.Column="6" x:Name="cmdSearch" Click="cmdSearch_Click"   ToolTipService.ToolTip="{Binding ttpSearch,Source={StaticResource locResource}}"  HorizontalAlignment="Left" >
                                        <Image Source="/PalashDynamics;component/Icons/Search_b.JPG" Margin="4,0,4,0" Width="20" ></Image>
                                    </Button>
                                </Grid>
                                <StackPanel controlsToolkit:DockPanel.Dock="Top" Margin="0,4,0,0">
                                    <Border  BorderBrush="{StaticResource TabControlDarkerBorder}"   BorderThickness="1" CornerRadius="3,3,0,0"  Background="Transparent">
                                        <ContentControl VerticalAlignment="Center" FontSize="12" Content="{Binding lblListofProcedures,Source={StaticResource locResource}}" Margin="-1" Style="{StaticResource Header}"     HorizontalAlignment="Stretch" />
                                    </Border>
                                </StackPanel>
                            </controlsToolkit:DockPanel>
                        </controlsToolkit:DockPanel>
                        <controlsToolkit:DockPanel LastChildFill="True">
                            <data:DataPager x:Name="dataGrid2Pager" controlsToolkit:DockPanel.Dock="Bottom" 
                                                        DisplayMode="FirstLastPreviousNext"  IsEnabled="True"                       
                                                        Source="{Binding}" 
                                                        AutoEllipsis="False" IsTotalItemCountFixed="False"  />
                            <data:DataGrid  x:Name="grdPathoTest" VerticalAlignment="Stretch" ItemsSource="{Binding}" AutoGenerateColumns="False" IsReadOnly="True" >
                                <data:DataGrid.Columns>
                                    <data:DataGridTemplateColumn Localization:DataGridColumnHelper.HeaderBinding="{Binding lblView,Source={StaticResource locResource}}">
                                        <data:DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <HyperlinkButton Margin="4" x:Name="cmdView" Content="{Binding lblView,Source={StaticResource locResource}}" Click="cmdView_Click" Cursor="Hand"></HyperlinkButton>
                                            </DataTemplate>
                                        </data:DataGridTemplateColumn.CellTemplate>
                                    </data:DataGridTemplateColumn>
                                    <data:DataGridTextColumn IsReadOnly="True" Localization:DataGridColumnHelper.HeaderBinding="{Binding lblCode,Source={StaticResource locResource}}" Binding="{Binding Code}" Width="100" />
                                    <data:DataGridTextColumn IsReadOnly="True"  Localization:DataGridColumnHelper.HeaderBinding="{Binding lblDescription,Source={StaticResource locResource}}"  Binding="{Binding Description}" Width="200"/>
                                    <data:DataGridTemplateColumn Localization:DataGridColumnHelper.HeaderBinding="{Binding lblStatus,Source={StaticResource locResource}}">
                                        <data:DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <CheckBox HorizontalAlignment="Center" Margin="4" IsChecked="{Binding Status,Mode=TwoWay}" Click="CheckBox_Click" IsEnabled="True"></CheckBox>
                                            </DataTemplate>
                                        </data:DataGridTemplateColumn.CellTemplate>
                                    </data:DataGridTemplateColumn>

                                </data:DataGrid.Columns>
                            </data:DataGrid>
                        </controlsToolkit:DockPanel>
                    </controlsToolkit:DockPanel>
                </Grid>
                <Grid Height="Auto" Visibility="Collapsed" Width="Auto" x:Name="BackPanel" Grid.Row="1">
                    <Border  BorderBrush="{StaticResource TabControlDarkerBorder}"    BorderThickness="1" CornerRadius="3,3,0,0" Margin="4,4,4,4"  Background="Transparent">
                        <controlsToolkit:DockPanel x:Name="Content" controlsToolkit:DockPanel.Dock="top" LastChildFill="true">
                            <controlsToolkit:DockPanel x:Name="docHeader" controlsToolkit:DockPanel.Dock="Top" HorizontalAlignment="Stretch" LastChildFill="true">
                                <GroupBox:GroupBox  Localization:DataGridColumnHelper.HeaderBinding="{Binding lblProcedureDetails,Source={StaticResource locResource}}" Margin="4" Width="Auto" Height="Auto" Grid.Row="0" HorizontalAlignment="Stretch">
                                    <Grid x:Name="grdBackPanel">
                                        <Grid.RowDefinitions>
                                            <RowDefinition></RowDefinition>
                                            <RowDefinition></RowDefinition>
                                            <RowDefinition></RowDefinition>
                                            <RowDefinition></RowDefinition>
                                            <RowDefinition></RowDefinition>
                                            <RowDefinition/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                                            <ColumnDefinition></ColumnDefinition>
                                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                                            <ColumnDefinition></ColumnDefinition>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock  Grid.Row="0" Grid.Column="0" Text="{Binding lblCode,Source={StaticResource locResource}}" Margin="4"></TextBlock>
                                        <TextBox  Grid.Row="0" Grid.Column="1" x:Name="txtCode" MaxLength="50" Text="{Binding Code,Mode=TwoWay}" Margin="4" LostFocus="txtCode_LostFocus" KeyDown="txtCode_KeyDown"  HorizontalAlignment="Stretch" ></TextBox>

                                        <TextBlock  Grid.Row="0" Grid.Column="2" Text="{Binding lblDescription,Source={StaticResource locResource}}" Margin="4"></TextBlock>
                                        <TextBox  Grid.Row="0" Grid.Column="3" MaxLength="100" x:Name="txtDescription" Margin="4" Text="{Binding Description,Mode=TwoWay}" HorizontalAlignment="Stretch" ></TextBox>

                                        <TextBlock  Grid.Row="1" Grid.Column="0" Text="{Binding lblServiceName,Source={StaticResource locResource}}" Margin="4" ></TextBlock>
                                        <inputcombo:AutoCompleteComboBox  Grid.Row="1" Grid.Column="1" Margin="4" x:Name="CmbServiceName"  SelectedItemBinding="{Binding MasterListItem, Mode=TwoWay}" SelectedValuePath="ID"  HorizontalAlignment="Stretch"></inputcombo:AutoCompleteComboBox >

                                        <TextBlock  Grid.Row="1" Grid.Column="2" Text="{Binding lblDuration,Source={StaticResource locResource}}" Margin="4" ></TextBlock>
                                        <TextBox  Grid.Row="1" Grid.Column="3" x:Name="txtDuration" Margin="4" Text="{Binding Duration,Mode=TwoWay}"  MaxLength="50" KeyDown="txtDuration_KeyDown"  LostFocus="txtDuration_LostFocus"   HorizontalAlignment="Stretch" ></TextBox>

                                        <TextBlock  Grid.Row="2" Grid.Column="0" Text="{Binding lblProcedureType,Source={StaticResource locResource}}" Margin="4" ></TextBlock>
                                        <inputcombo:AutoCompleteComboBox  Grid.Row="2" Grid.Column="1" Margin="4" x:Name="CmbProcedureType"  SelectedItemBinding="{Binding MasterListItem, Mode=TwoWay}" SelectedValuePath="ID"  HorizontalAlignment="Stretch"></inputcombo:AutoCompleteComboBox >

                                        <TextBlock  Grid.Row="2" Grid.Column="2" Text="{Binding lblRecommendedAnesthesiaType,Source={StaticResource locResource}}" Margin="4" ></TextBlock>
                                        <inputcombo:AutoCompleteComboBox  Grid.Row="2" Grid.Column="3" Margin="4" x:Name="CmbAnesthesiaType"  SelectedItemBinding="{Binding MasterListItem, Mode=TwoWay}" SelectedValuePath="ID"  HorizontalAlignment="Stretch"></inputcombo:AutoCompleteComboBox >

                                        <TextBlock  Grid.Row="3" Grid.Column="0" Text="{Binding lblOperationTheatre,Source={StaticResource locResource}}" Margin="4" ></TextBlock>
                                        <inputcombo:AutoCompleteComboBox  Grid.Row="3" Grid.Column="1" Margin="4" x:Name="CmbOT"  SelectedItemBinding="{Binding MasterListItem, Mode=TwoWay}" SelectedValuePath="ID"  HorizontalAlignment="Stretch" SelectionChanged="CmbOT_SelectionChanged"></inputcombo:AutoCompleteComboBox >

                                        <TextBlock  Grid.Row="3" Grid.Column="2" Text="{Binding lblOperationTable,Source={StaticResource locResource}}" Margin="4" ></TextBlock>
                                        <inputcombo:AutoCompleteComboBox  Grid.Row="3" Grid.Column="3" Margin="4" x:Name="CmbOTTable"  SelectedItemBinding="{Binding MasterListItem, Mode=TwoWay}" SelectedValuePath="ID"  HorizontalAlignment="Stretch"></inputcombo:AutoCompleteComboBox >

                                        <TextBlock  Grid.Row="4" Grid.Column="0" Text="{Binding lblRemark,Source={StaticResource locResource}}" Margin="4" ></TextBlock>
                                        <TextBox  Grid.Row="4" Grid.Column="1" x:Name="txtRemark" Margin="4,4,6,4" Text="{Binding Remark,Mode=TwoWay}"  MaxLength="100"   HorizontalAlignment="Stretch" Grid.ColumnSpan="3" Height="50" VerticalScrollBarVisibility="Visible" AcceptsReturn="True" TextWrapping="Wrap" ></TextBox>

                                    </Grid>
                                </GroupBox:GroupBox>
                            </controlsToolkit:DockPanel>
                            <controlsToolkit:DockPanel x:Name="docfooter" controlsToolkit:DockPanel.Dock="Bottom" HorizontalAlignment="Stretch" LastChildFill="True">


                                <controls:TabControl Padding="0"  BorderBrush="{StaticResource TabControlDarkerBorder}"  x:Name="TabControlMain" Margin="4,-4,4,4" controlsToolkit:DockPanel.Dock="Bottom">
                                    <controls:TabItem x:Name="CheckListDetails" Style="{StaticResource TabItemStyle}"  Padding="0" Margin="0" Header="{Binding tbhCheckList,Source={StaticResource locResource}}">
                                        <controlsToolkit:DockPanel>
                                            <controlsToolkit:DockPanel controlsToolkit:DockPanel.Dock="Bottom">
                                                <Grid x:Name="LayoutRoot" Background="White" Height="auto" Width="auto">
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="auto"></RowDefinition>
                                                    </Grid.RowDefinitions>

                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="auto"/>
                                                        <ColumnDefinition  />
                                                        <ColumnDefinition  Width="auto"/>
                                                        <ColumnDefinition />
                                                        <ColumnDefinition Width="auto" />
                                                        <ColumnDefinition />
                                                        <ColumnDefinition Width="auto" />
                                                    </Grid.ColumnDefinitions>

                                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding lblCategory,Source={StaticResource locResource}}" Margin="4" HorizontalAlignment="Right" VerticalAlignment="Center" />
                                                    <inputcombo:AutoCompleteComboBox  VerticalAlignment="Top" Margin="4" x:Name="CmbCategory" SelectedValuePath="ID" SelectedItemBinding="{Binding MasterListItem, Mode=TwoWay}" Grid.Row="0" Grid.Column="1" SelectionChanged="CmbCategory_SelectionChanged" ></inputcombo:AutoCompleteComboBox >
                                                    <TextBlock Grid.Column="2" Text="{Binding lblSubCategory,Source={StaticResource locResource}}" Margin="4" HorizontalAlignment="Right" VerticalAlignment="Center"  />
                                                    <inputcombo:AutoCompleteComboBox  VerticalAlignment="Top" Margin="4" x:Name="CmbSubCategory" SelectedValuePath="ID" SelectedItemBinding="{Binding MasterListItem, Mode=TwoWay}" Grid.Row="0" Grid.Column="3" SelectionChanged="CmbSubCategory_SelectionChanged" ></inputcombo:AutoCompleteComboBox >
                                                    <TextBlock Grid.Column="4" Text="{Binding lblCheckList,Source={StaticResource locResource}}" Margin="4" HorizontalAlignment="Right" VerticalAlignment="Center" />
                                                    <inputcombo:AutoCompleteComboBox  VerticalAlignment="Top" Margin="4" x:Name="CmbCheckList" SelectedValuePath="ID" SelectedItemBinding="{Binding MasterListItem, Mode=TwoWay}" Grid.Row="0" Grid.Column="5"></inputcombo:AutoCompleteComboBox >
                                                    <ToggleButton  Grid.Column="6" x:Name="btnAdd" Style="{StaticResource ToggleButtonStyle}"  Content="{Binding btnAdd,Source={StaticResource locResource}}" Margin="4" VerticalAlignment="Center" Click="btnAdd_Click"  ></ToggleButton>
                                                  
                                                </Grid>
                                            </controlsToolkit:DockPanel>
                                            <controlsToolkit:DockPanel LastChildFill="True" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                                                <data:DataGrid VerticalAlignment="Stretch" AutoGenerateColumns="False" Margin="4" Name="dgCheckList" ItemsSource="{Binding}">
                                                    <data:DataGrid.Columns>
                                                        <data:DataGridTextColumn IsReadOnly="True" Binding="{Binding Category}" Localization:DataGridColumnHelper.HeaderBinding="{Binding lblCategory,Source={StaticResource locResource}}" Visibility="Collapsed"/>
                                                        <data:DataGridTextColumn  IsReadOnly="True" Binding="{Binding SubCategory}" Localization:DataGridColumnHelper.HeaderBinding="{Binding lblSubCategory,Source={StaticResource locResource}}" Visibility="Collapsed" />
                                                        <data:DataGridTextColumn  IsReadOnly="True" Binding="{Binding Name}" Localization:DataGridColumnHelper.HeaderBinding="{Binding tbhCheckList,Source={StaticResource locResource}}" Visibility="Collapsed" />
                                                        <data:DataGridTextColumn  Binding="{Binding Name}" Localization:DataGridColumnHelper.HeaderBinding="{Binding lblName,Source={StaticResource locResource}}" />
                                                        <data:DataGridTemplateColumn Localization:DataGridColumnHelper.HeaderBinding="{Binding lblRemark,Source={StaticResource locResource}}" >
                                                            <data:DataGridTemplateColumn.CellTemplate >
                                                                <DataTemplate >
                                                                    <TextBox Margin="0" BorderThickness="0"  Background="Transparent"  x:Name="txtRemark"  Text="{Binding Remark,Mode=TwoWay}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" ></TextBox >
                                                                </DataTemplate>
                                                            </data:DataGridTemplateColumn.CellTemplate>
                                                        </data:DataGridTemplateColumn>
                                                        <data:DataGridTemplateColumn>
                                                            <data:DataGridTemplateColumn.CellTemplate>
                                                                <DataTemplate>
                                                                    <HyperlinkButton VerticalAlignment="Center" FontFamily="Webdings" Content="r"  Padding="5,0,5,0" IsTabStop="False" x:Name="cmdDeleteCheckListItem"  ToolTipService.ToolTip="Delete" Click="cmdDeleteCheckListItem_Click"></HyperlinkButton>
                                                                </DataTemplate>
                                                            </data:DataGridTemplateColumn.CellTemplate>
                                                        </data:DataGridTemplateColumn>
                                                    </data:DataGrid.Columns>
                                                </data:DataGrid>
                                            </controlsToolkit:DockPanel>
                                            
                                        </controlsToolkit:DockPanel>

                                    </controls:TabItem>
                                    <controls:TabItem x:Name="StaffDetails" Style="{StaticResource TabItemStyle}"  Padding="0" Margin="0" Header="{Binding tbhStaffDetails,Source={StaticResource locResource}}">
                                        <controls:TabControl Padding="0"  BorderBrush="{StaticResource TabControlDarkerBorder}"  x:Name="TabControlStaff" Margin="4,-4,4,4" controlsToolkit:DockPanel.Dock="Bottom">
                                            <controls:TabItem x:Name="DoctorDetails" Style="{StaticResource TabItemStyle}"  Padding="0" Margin="0" Header="{Binding tbhDoctor,Source={StaticResource locResource}}">
                                                <controlsToolkit:DockPanel x:Name="DocMain5" controlsToolkit:DockPanel.Dock="Top" LastChildFill="True">

                                                    <controlsToolkit:DockPanel x:Name="docContet22" controlsToolkit:DockPanel.Dock="Top" LastChildFill="True">
                                                        <controlsToolkit:DockPanel x:Name="docContet33" controlsToolkit:DockPanel.Dock="Bottom">
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                                                                    <ColumnDefinition ></ColumnDefinition>
                                                                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                                                                    <ColumnDefinition ></ColumnDefinition>
                                                                    <ColumnDefinition Width="Auto"></ColumnDefinition>

                                                                </Grid.ColumnDefinitions>
                                                                <Grid.RowDefinitions>
                                                                    <RowDefinition Height="Auto"></RowDefinition>
                                                                    <RowDefinition Height="Auto"></RowDefinition>
                                                                </Grid.RowDefinitions>

                                                                <TextBlock Text="{Binding lblSpecialization,Source={StaticResource locResource}}" Grid.Row="0" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="4"></TextBlock>
                                                                <inputcombo:AutoCompleteComboBox x:Name="cmbDoctorClassification"  ItemsSource="{Binding}" SelectedItemBinding="{Binding MasterListItem, Mode=TwoWay}" SelectedValuePath="ID"   Grid.Row="0" Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="4" TabIndex="7"></inputcombo:AutoCompleteComboBox>

                                                                <TextBlock Text="{Binding lblNo.,Source={StaticResource locResource}}" Grid.Row="0" Grid.Column="2" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="4" Visibility="Collapsed"></TextBlock>
                                                                <TextBox x:Name="txtDoctorQuantity"  Grid.Row="0" Grid.Column="3"  Margin="4" HorizontalAlignment="Stretch" VerticalAlignment="Center" KeyDown="txtDoctorQuantity_KeyDown"  LostFocus="txtDoctorQuantity_LostFocus"  MaxLength="2"   TabIndex="1" TextAlignment="Right" Text="{Binding xtx, StringFormat='N'}" Visibility="Collapsed"></TextBox>

                                                                <StackPanel x:Name="Footer11" Orientation="Horizontal" VerticalAlignment="Center"   Margin="0,3" HorizontalAlignment="left" Background="Transparent" Grid.Row="0" Grid.Column="5" >
                                                                    <StackPanel Orientation="Horizontal"  Margin="1" HorizontalAlignment="Right"  >
                                                                        <ToggleButton Content="{Binding btnAdd,Source={StaticResource locResource}}" x:Name="cmdAddStaff" Background="#000000" Margin="5,0,0,0" HorizontalAlignment="Stretch" Style="{StaticResource ToggleButtonStyle}" TabIndex="10" Click="cmdAddStaff_Click"></ToggleButton>
                                                                        <ToggleButton Content="{Binding btnModify,Source={StaticResource locResource}}" x:Name="cmdModifyStaff" IsEnabled="False" Background="#000000" Margin="5,0,0,0" HorizontalAlignment="Stretch" Style="{StaticResource ToggleButtonStyle}" TabIndex="10" Click="cmdModifyStaff_Click"></ToggleButton>

                                                                    </StackPanel>
                                                                </StackPanel>
                                                            </Grid>
                                                        </controlsToolkit:DockPanel>

                                                        <controlsToolkit:DockPanel x:Name="docContet12" controlsToolkit:DockPanel.Dock="Top">
                                                            <data:DataGrid x:Name="dgDocClassificationList" AutoGenerateColumns="False" ItemsSource="{Binding SampleList}" VerticalScrollBarVisibility="Visible" IsReadOnly="True" Margin="4" HorizontalAlignment="Stretch"  TabIndex="12" >
                                                                <data:DataGrid.Columns>
                                                                    <data:DataGridTextColumn Binding="{Binding DocClassification}"  Localization:DataGridColumnHelper.HeaderBinding="{Binding lblDoctor,Source={StaticResource locResource}}" ></data:DataGridTextColumn>
                                                                    <data:DataGridTextColumn Binding="{Binding NoofDocOrStaff,StringFormat=0.00,Mode=TwoWay}"  Localization:DataGridColumnHelper.HeaderBinding="{Binding lblNo,Source={StaticResource locResource}}" Visibility="Collapsed" ></data:DataGridTextColumn>
                                                                    <data:DataGridTemplateColumn>
                                                                        <data:DataGridTemplateColumn.CellTemplate>
                                                                            <DataTemplate>
                                                                                <HyperlinkButton VerticalAlignment="Center" FontFamily="Webdings" Content="r"  Padding="5,0,5,0" IsTabStop="False" x:Name="cmdDeleteDoc"  ToolTipService.ToolTip="Delete" Click="cmdDeleteDoc_Click" ></HyperlinkButton>
                                                                            </DataTemplate>
                                                                        </data:DataGridTemplateColumn.CellTemplate>
                                                                    </data:DataGridTemplateColumn>

                                                                    <data:DataGridTemplateColumn>
                                                                        <data:DataGridTemplateColumn.CellTemplate>
                                                                            <DataTemplate>
                                                                                <HyperlinkButton Background="White"  VerticalAlignment="Center" Padding="5,0,5,0" IsTabStop="False"  x:Name="hlbEditDoc" ToolTipService.ToolTip="Edit" Click="hlbEditDoc_Click">
                                                                                    <Image  Height="16" Stretch="Fill" Source="/PalashDynamics;component/Icons/edit_new.png" />
                                                                                </HyperlinkButton>
                                                                            </DataTemplate>
                                                                        </data:DataGridTemplateColumn.CellTemplate>
                                                                    </data:DataGridTemplateColumn>

                                                                </data:DataGrid.Columns>
                                                            </data:DataGrid>
                                                        </controlsToolkit:DockPanel>
                                                    </controlsToolkit:DockPanel>

                                                </controlsToolkit:DockPanel>
                                            </controls:TabItem>
                                            <controls:TabItem x:Name="StaffDetails1" Style="{StaticResource TabItemStyle}"  Padding="0" Margin="0" Header="{Binding tbhStaff,Source={StaticResource locResource}}">
                                                <controlsToolkit:DockPanel x:Name="DocMain6" controlsToolkit:DockPanel.Dock="Top" LastChildFill="True">

                                                    <controlsToolkit:DockPanel x:Name="docContet222" controlsToolkit:DockPanel.Dock="Top" LastChildFill="True">
                                                        <controlsToolkit:DockPanel x:Name="docContet333" controlsToolkit:DockPanel.Dock="Bottom">
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                                                                    <ColumnDefinition ></ColumnDefinition>
                                                                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                                                                    <ColumnDefinition ></ColumnDefinition>
                                                                    <ColumnDefinition Width="Auto"></ColumnDefinition>

                                                                </Grid.ColumnDefinitions>
                                                                <Grid.RowDefinitions>
                                                                    <RowDefinition Height="Auto"></RowDefinition>
                                                                    <RowDefinition Height="Auto"></RowDefinition>
                                                                </Grid.RowDefinitions>

                                                                <TextBlock Text="{Binding lblDesignation,Source={StaticResource locResource}}" Grid.Row="0" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="4"></TextBlock>
                                                                <inputcombo:AutoCompleteComboBox x:Name="cmbStaff"  ItemsSource="{Binding}" SelectedItemBinding="{Binding MasterListItem, Mode=TwoWay}" SelectedValuePath="ID"   Grid.Row="0" Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="4" TabIndex="7"></inputcombo:AutoCompleteComboBox>

                                                                <TextBlock Text="{Binding lblNo,Source={StaticResource locResource}}" Grid.Row="0" Grid.Column="2" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="4" Visibility="Collapsed"></TextBlock>
                                                                <TextBox x:Name="txtStaffQuantity"  Grid.Row="0" Grid.Column="3"  Margin="4" HorizontalAlignment="Stretch" VerticalAlignment="Center" KeyDown="txtDoctorQuantity_KeyDown" LostFocus="txtDoctorQuantity_LostFocus"  MaxLength="2"  TabIndex="1" TextAlignment="Right" Text="{Binding xtx, StringFormat='N'}" Visibility="Collapsed"></TextBox>
                                                                <StackPanel x:Name="Footer111" Orientation="Horizontal" VerticalAlignment="Center"   Margin="0,3" HorizontalAlignment="left" Background="Transparent" Grid.Row="0" Grid.Column="5" >
                                                                    <StackPanel Orientation="Horizontal"  Margin="1" HorizontalAlignment="Right"  >
                                                                        <ToggleButton Content="{Binding btnAdd,Source={StaticResource locResource}}" x:Name="cmdOtherStaffAdd" Background="#000000" Margin="5,0,0,0" HorizontalAlignment="Stretch" Style="{StaticResource ToggleButtonStyle}" TabIndex="10" Click="cmdOtherStaffAdd_Click" ></ToggleButton>
                                                                        <ToggleButton Content="{Binding btnModify,Source={StaticResource locResource}}" x:Name="cmdModifyStaffOther" Background="#000000" Margin="5,0,0,0" HorizontalAlignment="Stretch" IsEnabled="False" Style="{StaticResource ToggleButtonStyle}" TabIndex="10" Click="cmdModifyStaffOther_Click"></ToggleButton>

                                                                    </StackPanel>
                                                                </StackPanel>
                                                            </Grid>
                                                        </controlsToolkit:DockPanel>

                                                        <controlsToolkit:DockPanel x:Name="docContet122" controlsToolkit:DockPanel.Dock="Top">
                                                            <data:DataGrid x:Name="dgStaffList" AutoGenerateColumns="False" ItemsSource="{Binding SampleList}" VerticalScrollBarVisibility="Visible" IsReadOnly="True" Margin="4" HorizontalAlignment="Stretch"  TabIndex="12" >
                                                                <data:DataGrid.Columns>
                                                                    <data:DataGridTextColumn Binding="{Binding DocClassification}"  Localization:DataGridColumnHelper.HeaderBinding="{Binding tbhStaff,Source={StaticResource locResource}}" ></data:DataGridTextColumn>
                                                                    <data:DataGridTextColumn Binding="{Binding NoofDocOrStaff,StringFormat=0.00,Mode=TwoWay}"  Localization:DataGridColumnHelper.HeaderBinding="{Binding lblNo,Source={StaticResource locResource}}" Visibility="Collapsed" ></data:DataGridTextColumn>
                                                                    <data:DataGridTemplateColumn>
                                                                        <data:DataGridTemplateColumn.CellTemplate>
                                                                            <DataTemplate>
                                                                                <HyperlinkButton VerticalAlignment="Center" FontFamily="Webdings" Content="r"  Padding="5,0,5,0" IsTabStop="False" x:Name="cmdDeleteStaffOther"  ToolTipService.ToolTip="Delete" Click="cmdDeleteStaffOther_Click"></HyperlinkButton>
                                                                            </DataTemplate>
                                                                        </data:DataGridTemplateColumn.CellTemplate>
                                                                    </data:DataGridTemplateColumn>
                                                                    <data:DataGridTemplateColumn>
                                                                        <data:DataGridTemplateColumn.CellTemplate>
                                                                            <DataTemplate>
                                                                                <HyperlinkButton Background="White"  VerticalAlignment="Center" Padding="5,0,5,0" IsTabStop="False"  x:Name="hlbEditStaffOther" ToolTipService.ToolTip="Edit" Click="hlbEditStaffOther_Click">
                                                                                    <Image  Height="16" Stretch="Fill" Source="/PalashDynamics;component/Icons/edit_new.png" />
                                                                                </HyperlinkButton>
                                                                            </DataTemplate>
                                                                        </data:DataGridTemplateColumn.CellTemplate>
                                                                    </data:DataGridTemplateColumn>
                                                                </data:DataGrid.Columns>
                                                            </data:DataGrid>
                                                        </controlsToolkit:DockPanel>
                                                    </controlsToolkit:DockPanel>

                                                </controlsToolkit:DockPanel>
                                            </controls:TabItem>
                                        </controls:TabControl>
                                    </controls:TabItem>

                                    <controls:TabItem x:Name="InstructionDetails" Style="{StaticResource TabItemStyle}"  Padding="0" Margin="0" Header="{Binding tbhInstructions,Source={StaticResource locResource}}">
                                        <controls:TabControl Padding="0"  BorderBrush="{StaticResource TabControlDarkerBorder}"  x:Name="TabControlInstruction1" Margin="4,-4,4,4" controlsToolkit:DockPanel.Dock="Bottom">
                                            <controls:TabItem x:Name="PreOperative" Style="{StaticResource TabItemStyle}"  Padding="0" Margin="0" Header="{Binding tbhPreOperativeInstruction,Source={StaticResource locResource}}">
                                                <ContentControl Template="{StaticResource FilledContentControlTemplate}"  Background="Aqua"  x:Name="PreOPResultListContent" ScrollViewer.VerticalScrollBarVisibility="Visible"   ScrollViewer.HorizontalScrollBarVisibility="Auto" />
                                            </controls:TabItem>
                                            <controls:TabItem x:Name="IntraOperative" Style="{StaticResource TabItemStyle}"  Padding="0" Margin="0" Header="{Binding tbhIntraOperativeInstruction,Source={StaticResource locResource}}">                                                
                                                <ContentControl Template="{StaticResource FilledContentControlTemplate}"  Background="Aqua"  x:Name="IntraOPResultListContent" ScrollViewer.VerticalScrollBarVisibility="Visible"   ScrollViewer.HorizontalScrollBarVisibility="Auto" />
                                            </controls:TabItem>
                                            <controls:TabItem x:Name="PostOperative" Style="{StaticResource TabItemStyle}"  Padding="0" Margin="0" Header="{Binding tbhPostOperativeInstruction,Source={StaticResource locResource}}">
                                                <ContentControl Template="{StaticResource FilledContentControlTemplate}"  Background="Aqua"  x:Name="PostOPResultListContent" ScrollViewer.VerticalScrollBarVisibility="Visible"   ScrollViewer.HorizontalScrollBarVisibility="Auto" />
                                            </controls:TabItem>
                                        </controls:TabControl>
                                    </controls:TabItem>
                                    <controls:TabItem x:Name="ConsentDetails" Style="{StaticResource TabItemStyle}"  Padding="0" Margin="0" Header="{Binding tbhConsent,Source={StaticResource locResource}}">
                                        <ContentControl Template="{StaticResource FilledContentControlTemplate}"  Background="Aqua"  x:Name="ConsentResultListContent" ScrollViewer.VerticalScrollBarVisibility="Visible"   ScrollViewer.HorizontalScrollBarVisibility="Auto" />
                                    </controls:TabItem>

                                    <controls:TabItem x:Name="ItemDetails" Style="{StaticResource TabItemStyle}"  Padding="0" Margin="0" Header="{Binding tbhItemDetails,Source={StaticResource locResource}}">
                                        
                                                <controlsToolkit:DockPanel x:Name="DocItem" controlsToolkit:DockPanel.Dock="Top" LastChildFill="True">
                                                    <controlsToolkit:DockPanel x:Name="docItemBot" controlsToolkit:DockPanel.Dock="Bottom">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition></ColumnDefinition>
                                                            </Grid.ColumnDefinitions>
                                                            <Grid.RowDefinitions>
                                                                <RowDefinition></RowDefinition>
                                                            </Grid.RowDefinitions>
                                                            <ToggleButton Content="{Binding btnAdd,Source={StaticResource locResource}}" x:Name="cmdAddItem" Background="#000000" Margin="4" HorizontalAlignment="Left" Style="{StaticResource ToggleButtonStyle}" TabIndex="10" Click="cmdAddItem_Click"></ToggleButton>
                                                        </Grid>
                                                    </controlsToolkit:DockPanel>
                                                    <controlsToolkit:DockPanel x:Name="docItemTop" controlsToolkit:DockPanel.Dock="Top">
                                                        <data:DataGrid x:Name="dgItemDetailsList" AutoGenerateColumns="False" ItemsSource="{Binding ItemList}" VerticalScrollBarVisibility="Visible"  Margin="4" HorizontalAlignment="Stretch"  TabIndex="12" >
                                                            <data:DataGrid.Columns>

                                                                <data:DataGridTextColumn Binding="{Binding ItemName}"  Localization:DataGridColumnHelper.HeaderBinding="{Binding lblItemName,Source={StaticResource locResource}}" IsReadOnly="True"></data:DataGridTextColumn>
                                                                <data:DataGridTemplateColumn Localization:DataGridColumnHelper.HeaderBinding="{Binding lblQuantity,Source={StaticResource locResource}}">
                                                                    <data:DataGridTemplateColumn.CellTemplate>
                                                                        <DataTemplate>
                                                                            <TextBox x:Name="txtQuantity" KeyDown="txtDoctorQuantity_KeyDown"  MaxLength="10" LostFocus="txtDoctorQuantity_LostFocus" Text="{Binding Quantity,Mode=TwoWay,StringFormat=0.00}"></TextBox>
                                                                        </DataTemplate>
                                                                    </data:DataGridTemplateColumn.CellTemplate>
                                                                </data:DataGridTemplateColumn>

                                                                <data:DataGridTemplateColumn Localization:DataGridColumnHelper.HeaderBinding="{Binding lblDelete,Source={StaticResource locResource}}">
                                                                    <data:DataGridTemplateColumn.CellTemplate>
                                                                        <DataTemplate>
                                                                            <HyperlinkButton VerticalAlignment="Center" FontFamily="Webdings" Content="r"  Padding="5,0,5,0" IsTabStop="False" x:Name="cmdDeleteItem"  ToolTipService.ToolTip="Delete" Click="cmdDeleteItem_Click"></HyperlinkButton>
                                                                        </DataTemplate>
                                                                    </data:DataGridTemplateColumn.CellTemplate>
                                                                </data:DataGridTemplateColumn>
                                                            </data:DataGrid.Columns>
                                                        </data:DataGrid>
                                                    </controlsToolkit:DockPanel>

                                                </controlsToolkit:DockPanel>
                                           
                                    </controls:TabItem>
                                    
                                    
                                    <controls:TabItem x:Name="ServiceDetails" Style="{StaticResource TabItemStyle}"  Padding="0" Margin="0" Header="{Binding tbhServiceDetails,Source={StaticResource locResource}}">
                                        <controlsToolkit:DockPanel x:Name="DocService" controlsToolkit:DockPanel.Dock="Top" LastChildFill="True">
                                            <controlsToolkit:DockPanel x:Name="docServiceButton" controlsToolkit:DockPanel.Dock="Bottom">
                                                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Margin="4">
                                                    <ToggleButton Content="{Binding btnLab,Source={StaticResource locResource}}" x:Name="cmdLab" Margin="4" HorizontalAlignment="Left" Style="{StaticResource ToggleButtonStyle}"  Click="cmdLab_Click"></ToggleButton>
                                                    <ToggleButton Content="{Binding btnRadiology,Source={StaticResource locResource}}" x:Name="cmdRadiology" Margin="4" HorizontalAlignment="Left" Style="{StaticResource ToggleButtonStyle}"  Click="cmdRadiology_Click"></ToggleButton>
                                                    <!--<ToggleButton Content="Diagnostik" x:Name="cmdDiagnostik" Margin="4" HorizontalAlignment="Left" Style="{StaticResource ToggleButtonStyle}"  Click="cmdDiagnostik_Click"></ToggleButton>-->
                                                </StackPanel>
                                            </controlsToolkit:DockPanel>
                                            <controlsToolkit:DockPanel x:Name="docServiceTop" controlsToolkit:DockPanel.Dock="Top">

                                                <data:DataGrid x:Name="dgServiceList" HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" Grid.ColumnSpan="2"  AutoGenerateColumns="False" ItemsSource="{Binding}" IsReadOnly="True" Margin="0"  TabIndex="2"  CanUserSortColumns="True" >
                                                    <data:DataGrid.Columns>
                                                        <data:DataGridTextColumn Localization:DataGridColumnHelper.HeaderBinding="{Binding lblServiceCode,Source={StaticResource locResource}}" Binding="{Binding ServiceCode}" CanUserResize="False"></data:DataGridTextColumn>
                                                        <data:DataGridTextColumn Localization:DataGridColumnHelper.HeaderBinding="{Binding lblServiceName,Source={StaticResource locResource}}" Width="350" Binding="{Binding ServiceName}" ></data:DataGridTextColumn>

                                                        <data:DataGridTemplateColumn Localization:DataGridColumnHelper.HeaderBinding="{Binding lblDelete,Source={StaticResource locResource}}">
                                                            <data:DataGridTemplateColumn.CellTemplate>
                                                                <DataTemplate>
                                                                    <HyperlinkButton VerticalAlignment="Center" FontFamily="Webdings" Content="r"  Padding="5,0,5,0" IsTabStop="False" x:Name="cmdDeleteService"  ToolTipService.ToolTip="Delete" Click="cmdDeleteService_Click"></HyperlinkButton>
                                                                </DataTemplate>
                                                            </data:DataGridTemplateColumn.CellTemplate>
                                                        </data:DataGridTemplateColumn>
                                                    </data:DataGrid.Columns>
                                                </data:DataGrid>
                                                
                                                
                                            </controlsToolkit:DockPanel>

                                        </controlsToolkit:DockPanel>
                                        
                                    </controls:TabItem>

                                    <controls:TabItem x:Name="ProcedureTempateDetails" Style="{StaticResource TabItemStyle}"  Padding="0" Margin="0" Header="Procedure Template Details">
                                        <controlsToolkit:DockPanel x:Name="DocProcedureTempate" controlsToolkit:DockPanel.Dock="Top" LastChildFill="True">
                                            <controlsToolkit:DockPanel x:Name="docProcedureTempateBot" controlsToolkit:DockPanel.Dock="Bottom">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="auto"></ColumnDefinition>
                                                        <ColumnDefinition ></ColumnDefinition>
                                                        <ColumnDefinition Width="auto"></ColumnDefinition>
                                                    </Grid.ColumnDefinitions>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition></RowDefinition>
                                                    </Grid.RowDefinitions>
                                                    <TextBlock Text="Template Name" Grid.Row="0" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="4"></TextBlock>
                                                    <inputcombo:AutoCompleteComboBox x:Name="cmbTempate"  ItemsSource="{Binding}" SelectedItemBinding="{Binding MasterListItem, Mode=TwoWay}" SelectedValuePath="ID"   Grid.Row="0" Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="4" TabIndex="7"></inputcombo:AutoCompleteComboBox>

                                                    <StackPanel x:Name="FooterProcTemp" Orientation="Horizontal" VerticalAlignment="Center"   Margin="0,3" HorizontalAlignment="left" Background="Transparent" Grid.Row="0" Grid.Column="5" >
                                                        <StackPanel Orientation="Horizontal"  Margin="1" HorizontalAlignment="Right"  >
                                                            <ToggleButton Content="{Binding btnAdd,Source={StaticResource locResource}}" x:Name="cmdAddProcedureTemp" Background="#000000" Margin="5,0,0,0" HorizontalAlignment="Stretch" Style="{StaticResource ToggleButtonStyle}" TabIndex="10" Click="cmdAddProcedureTemp_Click"></ToggleButton>
                                                            <ToggleButton Content="{Binding btnModify,Source={StaticResource locResource}}" x:Name="cmdModifyProcedureTemp" Background="#000000" Margin="5,0,0,0" IsEnabled="False" HorizontalAlignment="Stretch" Style="{StaticResource ToggleButtonStyle}" TabIndex="10" Click="cmdModifyProcedureTemp_Click"></ToggleButton>

                                                        </StackPanel>
                                                    </StackPanel>
                                                </Grid>
                                            </controlsToolkit:DockPanel>
                                            <controlsToolkit:DockPanel x:Name="docProcedureTempate" controlsToolkit:DockPanel.Dock="Top">
                                                <data:DataGrid x:Name="dgProcedureTempate" AutoGenerateColumns="False" ItemsSource="{Binding SampleList}" IsReadOnly="True" VerticalScrollBarVisibility="Visible"  Margin="4" HorizontalAlignment="Stretch"  TabIndex="12" >
                                                    <data:DataGrid.Columns>

                                                        <data:DataGridTextColumn Binding="{Binding Description}"  Header="Template Name" IsReadOnly="True"></data:DataGridTextColumn>

                                                        <data:DataGridTemplateColumn Header="Delete">
                                                            <data:DataGridTemplateColumn.CellTemplate>
                                                                <DataTemplate>
                                                                    <HyperlinkButton VerticalAlignment="Center" FontFamily="Webdings" Content="r"  Padding="5,0,5,0" IsTabStop="False" x:Name="cmdDeleteTempateProc"  ToolTipService.ToolTip="Delete" Click="cmdDeleteTempateProc_Click" ></HyperlinkButton>
                                                                </DataTemplate>
                                                            </data:DataGridTemplateColumn.CellTemplate>
                                                        </data:DataGridTemplateColumn>

                                                        <data:DataGridTemplateColumn Header="Modify">
                                                            <data:DataGridTemplateColumn.CellTemplate>
                                                                <DataTemplate>
                                                                    <HyperlinkButton Background="White"  VerticalAlignment="Center" Padding="5,0,5,0" IsTabStop="False"  x:Name="hlbEditDeleteTempateProc" ToolTipService.ToolTip="Edit" Click="hlbEditDeleteTempateProc_Click">
                                                                        <Image  Height="16" Stretch="Fill" Source="/PalashDynamics;component/Icons/edit_new.png" />
                                                                    </HyperlinkButton>
                                                                </DataTemplate>
                                                            </data:DataGridTemplateColumn.CellTemplate>
                                                        </data:DataGridTemplateColumn>
                                                    </data:DataGrid.Columns>
                                                </data:DataGrid>
                                            </controlsToolkit:DockPanel>

                                        </controlsToolkit:DockPanel>
                                    </controls:TabItem>

                                </controls:TabControl>
                            </controlsToolkit:DockPanel>
                        </controlsToolkit:DockPanel>
                    </Border>
                </Grid>
            </Grid>
        </controlsToolkit:DockPanel>
    </Grid>
</UserControl>
