<UserControl x:Class="PalashDynamics.Administration.SearchResultList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:my="clr-namespace:PalashDynamics.Controls;assembly=PalashDynamics.Controls.AutoCompleteComboBox"  
    xmlns:toolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk"
    xmlns:Localization="clr-namespace:PalashDynamic.Localization;assembly=PalashDynamic.Localization"
    mc:Ignorable="d"
    Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/PalashDynamics;component/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Localization:Localized x:Key="locResource"></Localization:Localized>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid x:Name="LayoutRoot" Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition ></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>

        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition  Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions> <TextBlock Grid.Column="0" Grid.Row="0" Text="Search" Margin="4" Visibility="Collapsed" x:Name="txblSearch"/>
        <ToggleButton  Margin="4"  Content="{Binding btnSearch,Source={StaticResource locResource}}"  x:Name="cmdAdd" Click="cmdAdd_Click" Grid.Column="1" Grid.Row="1" Visibility="Collapsed" ></ToggleButton>
        <TextBox Grid.Column="0" Grid.Row="1" Margin="4" x:Name="cmbSearch" Visibility="Collapsed" HorizontalAlignment="Stretch"/>

        <Grid Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition ></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition ></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition ></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition ></RowDefinition>
            </Grid.RowDefinitions>
            <toolkit:DockPanel  Grid.Column="0" Grid.Row="0" Grid.RowSpan="6" LastChildFill="True">
                <sdk:DataPager x:Name="dataGrid2Pager" toolkit:DockPanel.Dock="Bottom" 
                                                        DisplayMode="FirstLastPreviousNext"  IsEnabled="True"                       
                                                        Source="{Binding}" 
                                                        AutoEllipsis="False" IsTotalItemCountFixed="True"  />
                <sdk:DataGrid  x:Name="dgSearch1" ItemsSource="{Binding}" AutoGenerateColumns="False"   Margin="4" HorizontalAlignment="Stretch" >
                    <sdk:DataGrid.Columns>
                        <sdk:DataGridTemplateColumn >
                            <sdk:DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox HorizontalAlignment="Center" Margin="4" IsChecked ="{Binding Status,Mode=TwoWay}"></CheckBox>
                                </DataTemplate>
                            </sdk:DataGridTemplateColumn.CellTemplate>

                        </sdk:DataGridTemplateColumn>
                        <sdk:DataGridTextColumn IsReadOnly="True" Localization:DataGridColumnHelper.HeaderBinding="{Binding lblTest, Source={StaticResource locResource}}"  Binding="{Binding Description}" x:Name="colDescription"  />

                    </sdk:DataGrid.Columns>
                </sdk:DataGrid>
            </toolkit:DockPanel>

            <ToggleButton Grid.Row="1" Grid.Column="1"  Margin="4" ToolTipService.ToolTip="{Binding lblAddAll, Source={StaticResource locResource}}"  Content=">>" x:Name="cmdAddAll" Click="cmdAddAll_Click"  Width="25" Height="20" VerticalAlignment="Center"></ToggleButton>
            <ToggleButton  Grid.Row="2"  Grid.Column="1" Margin="4" ToolTipService.ToolTip="{Binding lblAdd, Source={StaticResource locResource}}" Content=">" x:Name="cmdAdd1" Click="cmdAdd1_Click"  Width="25" Height="20" VerticalAlignment="Center"></ToggleButton>
            <ToggleButton  Grid.Row="3"  Grid.Column="1" Margin="4" ToolTipService.ToolTip="{Binding lblRemove, Source={StaticResource locResource}}" Content="&lt;" x:Name="cmdRemove" Click="cmdRemove_Click"  Width="25" Height="20" VerticalAlignment="Center"></ToggleButton>
            <ToggleButton  Grid.Row="4"  Grid.Column="1" Margin="4" ToolTipService.ToolTip="{Binding lblRemoveAll, Source={StaticResource locResource}}" Content="&lt;&lt;" x:Name="cmdRemoveAll" Click="cmdRemoveAll_Click"  Width="25" Height="20" VerticalAlignment="Center"></ToggleButton>

            <sdk:DataGrid  x:Name="dgSearch2" ItemsSource="{Binding}" AutoGenerateColumns="False" Grid.Row="0" Grid.RowSpan="6" Grid.Column="2" Margin="4" HorizontalAlignment="Stretch">
                <sdk:DataGrid.Columns>
                    <sdk:DataGridTemplateColumn >
                        <sdk:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox HorizontalAlignment="Center" Margin="4" IsChecked ="{Binding Status,Mode=TwoWay}" Checked="dgSearch2ChkBox_Checked" Unchecked="dgSearch2ChkBox_UnChecked"></CheckBox>
                            </DataTemplate>
                        </sdk:DataGridTemplateColumn.CellTemplate>

                    </sdk:DataGridTemplateColumn>
                    <sdk:DataGridTextColumn IsReadOnly="True" Localization:DataGridColumnHelper.HeaderBinding="{Binding lblTest, Source={StaticResource locResource}}" Binding="{Binding Description}" x:Name="ColDg2Description" />
                </sdk:DataGrid.Columns>
            </sdk:DataGrid>
        </Grid>
    </Grid>
</UserControl>
