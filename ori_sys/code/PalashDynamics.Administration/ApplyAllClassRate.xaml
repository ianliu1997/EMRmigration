<controls:ChildWindow x:Class="PalashDynamics.Administration.ApplyAllClassRate"
           xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk"  
        xmlns:toolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
        xmlns:controls="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls"
        xmlns:Grp="clr-namespace:Groupbox;assembly=Groupbox"
        xmlns:inputcombo="clr-namespace:PalashDynamics.Controls;assembly=PalashDynamics.Controls.AutoCompleteComboBox"
        xmlns:local="clr-namespace:PalashDynamics.Converters;assembly=PalashDynamics.Converters"     
        Width="450" Height="Auto"
        Title="" Loaded="ChildWindow_Loaded">

    <controls:ChildWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/PalashDynamics;component/Generic.xaml"/>

            </ResourceDictionary.MergedDictionaries>
            <local:IDToDescriptionConvertor  x:Key="IDToDescription" ></local:IDToDescriptionConvertor>
        </ResourceDictionary>

    </controls:ChildWindow.Resources>
    <Grid x:Name="LayoutRoot" Height="Auto">
        <toolkit:DockPanel x:Name="MainDock" LastChildFill="True" toolkit:DockPanel.Dock="Top">
            <toolkit:DockPanel toolkit:DockPanel.Dock="Top" LastChildFill="True">
                <toolkit:DockPanel toolkit:DockPanel.Dock="Bottom" Margin="4">
                    <StackPanel x:Name="FooterBackPanel" Orientation="Horizontal"  Margin="0,3,5,3" HorizontalAlignment="Right" Background="Transparent">
                        <StackPanel x:Name="SaveCommandPanelBackPanel" Orientation="Horizontal"  Margin="0,0,0,0">
                            <ToggleButton x:Name="OKButton" Click="OKButton_Click"  Content="OK"   Background="#000000" Margin="5,0,0,0" HorizontalAlignment="Stretch" Style="{StaticResource ToggleButtonStyle}" Visibility="Visible"  />
                            <ToggleButton x:Name="CancelButton" Click="CancelButton_Click"  Content="Cancel"  Background="#000000" Margin="5,0,0,0" HorizontalAlignment="Stretch" Style="{StaticResource ToggleButtonStyle}" Visibility="Visible"   />
                        </StackPanel>
                    </StackPanel>
                </toolkit:DockPanel>
                <Grp:GroupBox Header="">
                    <toolkit:DockPanel toolkit:DockPanel.Dock="Top" >
                        <Grp:GroupBox Header="Apply Class Rate To All Tariff" >
                            <!--<Grid toolkit:DockPanel.Dock="Top">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="10"></RowDefinition>
                                    <RowDefinition Height="25"></RowDefinition>
                                    <RowDefinition Height="25"></RowDefinition>
                                    <RowDefinition Height="25"></RowDefinition>
                                    <RowDefinition Height="05"></RowDefinition>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto" ></ColumnDefinition>
                                    <ColumnDefinition Width="auto" ></ColumnDefinition>
                                    <ColumnDefinition Width="auto" ></ColumnDefinition>
                                    <ColumnDefinition Width="auto" ></ColumnDefinition>
                                </Grid.ColumnDefinitions>
                                <RadioButton x:Name="rdbNew" Content="Link With Tariff / Package" Margin="65,0,2,1" IsChecked="True"  HorizontalAlignment="Left" Grid.Row="1" Grid.Column="1"></RadioButton>
                                <RadioButton x:Name="rdbModify" Content="Change Rate Of Existing Tariff / Package" Margin="65,0,2,1" HorizontalAlignment="Left" Grid.Row="2" Grid.Column="1"></RadioButton>
                                <RadioButton x:Name="rdbRemove" Content="Remove Linking Of Existing Tariff / Package" Margin="65,0,2,1" HorizontalAlignment="Left"  Grid.Row="3" Grid.Column="1"></RadioButton>

                            </Grid>-->

                            <Grid Grid.Column="1"  Margin="4" >
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"></RowDefinition>
                                    <RowDefinition Height="Auto"></RowDefinition>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto"></ColumnDefinition>
                                    <ColumnDefinition Width="auto"></ColumnDefinition>
                                </Grid.ColumnDefinitions>
                                <CheckBox x:Name="chkApplyBaseRate" Content="Apply Base Rate" Margin="2" Click="ApplyBaseRate_Click" Grid.Row="0" Grid.Column="0"></CheckBox>
                                <sdk:DataGrid  x:Name="grdClass" VerticalAlignment="Stretch" Grid.Row="1" Visibility="Visible" Grid.Column="0" ItemsSource="{Binding}" AutoGenerateColumns="False" IsReadOnly="True">
                                    <sdk:DataGrid.Columns>
                                        <sdk:DataGridTemplateColumn Header="Select" >
                                            <sdk:DataGridTemplateColumn.CellTemplate >
                                                <DataTemplate >
                                                    <CheckBox Margin="10,2,10,2"  x:Name="chkSelectClass" IsEnabled="{Binding IsEnabled,Mode=TwoWay}"  IsTabStop="True" Click="chkSelectClass_Click" IsChecked="{Binding IsSelected,Mode=TwoWay}" VerticalAlignment="Center" HorizontalAlignment="Center" ></CheckBox >
                                                </DataTemplate>
                                            </sdk:DataGridTemplateColumn.CellTemplate>
                                        </sdk:DataGridTemplateColumn>
                                        <sdk:DataGridTextColumn IsReadOnly="True"  Header="Class Name"  Binding="{Binding ClassName}" Width="200"/>
                                        <sdk:DataGridTemplateColumn Header="Rate" Width="150">
                                            <sdk:DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBox Margin="4" x:Name="txtServiceClassRate"  Text="{Binding Rate,StringFormat=0.00,Mode=TwoWay}" IsTabStop="True" Width="130" VerticalAlignment="Center" HorizontalAlignment="Center" IsReadOnly="{Binding IsClassRateReadonly,Mode=TwoWay}"></TextBox>
                                                </DataTemplate>
                                            </sdk:DataGridTemplateColumn.CellTemplate>
                                        </sdk:DataGridTemplateColumn>
                                    </sdk:DataGrid.Columns>
                                </sdk:DataGrid>
                            </Grid>
                        </Grp:GroupBox>
                    </toolkit:DockPanel>
                </Grp:GroupBox>

            </toolkit:DockPanel>
        </toolkit:DockPanel>
    </Grid>
</controls:ChildWindow>