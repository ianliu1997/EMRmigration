<UserControl x:Class="OPDModule.Forms.frmTokenDisplay"
    xmlns:toolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit" 
    xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"    
    xmlns:data="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Data"
    xmlns:controlsToolkit="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Toolkit"
    xmlns:controls="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls"
             
    xmlns:common="clr-namespace:System.Windows;assembly=System.Windows.Controls" 
     xmlns:Converter="clr-namespace:PalashDynamics.Converters;assembly=PalashDynamics.Converters"
    Loaded="frmTokenDisplay_Loaded">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/PalashDynamics;component/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Converter:DateTimeToAgePart x:Key="DateTimeToAgePart"></Converter:DateTimeToAgePart>
            <Converter:DateTimeConverter x:Key="DateConverter"></Converter:DateTimeConverter>
            <Converter:ToggleBooleanValueConverter x:Key="NegateValueConverter"/>
            <Converter:VisiblilityConverter x:Key="VisiblilityConverter"/>
            <ControlTemplate x:Key="FilledContentControlTemplate" TargetType="ContentControl">
                <Grid>
                    <ContentPresenter />
                </Grid>
            </ControlTemplate>
            <Style TargetType="data:DataGridCell" x:Key="DataGridCellStyle">
                <Setter Property="Foreground" Value="#008000"/>
                <Setter Property="FontStyle" Value="Normal"/>
                <Setter Property="FontFamily" Value="Arial"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
            </Style>

        </ResourceDictionary>
    </UserControl.Resources>

    <Grid x:Name="LayoutRoot" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <data:DataGrid Grid.Row="0" x:Name="dgTokenDisp" Margin="0,0,0,1" ItemsSource="{Binding}"  AutoGenerateColumns="False" CanUserSortColumns="False" >
            <data:DataGrid.Columns >
                <data:DataGridTextColumn Binding="{Binding TokenNo, Mode=OneWay}" Header="Token No." IsReadOnly="True" Width="120" Foreground="#FF00F900" />
                <data:DataGridTextColumn Binding="{Binding Cabin,Mode=OneWay}"  Header="Room No." IsReadOnly="True" Width="120" Foreground="#FF00F900" />
                <data:DataGridTextColumn Binding="{Binding MrNo,Mode=OneWay}"  Header="Registration No." IsReadOnly="True" Width="120" Foreground="#FF00F900" />
                <data:DataGridTextColumn Binding="{Binding PatientName,Mode=OneWay}"  Header="Patient Name" IsReadOnly="True" Width="200" Foreground="#FF00F900" />
                <data:DataGridTextColumn Binding="{Binding DoctorName,Mode=OneWay}"  Header="Doctor Name" IsReadOnly="True" Width="200" Foreground="#FF00F900" />
            </data:DataGrid.Columns>
        </data:DataGrid>
        <!--<MediaElement x:Name="MediaAudio" Source="/OPDModule;component/Forms/AudioFile/beep-9.wav"  Width="00" Height="0" />-->
        <MediaElement x:Name="MediaAudio" Source="AudioFile/Wildlife.wmv"  Width="00" Height="0" Visibility="Collapsed" />

    </Grid>
</UserControl>
